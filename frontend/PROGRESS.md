# PROGRESS.md - DineFlow POS Frontend

Dokumen progres & roadmap pengembangan frontend. **Baca di awal setiap sesi**, update checklist & log setelah menyelesaikan step baru.

## 1. Status Keseluruhan

| # | Step | Status | Catatan |
|---|---|---|---|
| 1 | Scaffold proyek (Vite + React + TS + Tailwind) | ✅ Selesai | Token DESIGN.md di `src/index.css`, alias `@` terpasang |
| 2 | Kasir `/kasir` | ✅ Selesai | 2 kolom, keranjang, pembayaran tunai/QRIS |
| 3 | Pelayan `/pelayan` | ✅ Selesai | Mobile-first: pilih meja → input → kirim → daftar pesanan |
| 4 | Kitchen Display `/kitchen` | ✅ Selesai | Grid ticket, update status per item, polling 5s (simulasi real-time) |
| 5 | Pesan Mandiri QR `/menu/:table` | ✅ Selesai | Katalog, keranjang, tracking status pesanan |
| 6 | Admin `/admin` | ✅ Selesai | 4 tab: Manajemen Menu, Meja (+QR), Staf, Laporan (filter periode) |
| 7 | Sambungkan ke API Laravel | ✅ Selesai | Ganti `MockApi` di `src/services/mockApi.ts` dengan fetch/axios, UI tidak berubah. Selesai di Fase B (`httpApi.ts` aktif sebagai `api`; `mockApi.ts` hanya dipakai tes service layer) |
| 8 | Real-time Laravel Reverb + Redis | ✅ Selesai | Echo + Reverb (B2): ganti polling 5s di KitchenPage, KasirPage, MenuPage tracking. Event `OrderStatusChanged` → channel `orders` + `order.{orderNumber}`. NFR ≤2s terverifikasi |
| 9 | Auth & role (kasir/pelayan/dapur/admin) | ✅ Selesai (mock) | Login + guard role + logout, akun demo (password 1234). Auth nyata (Sanctum) tetap di rencana B |
| 10 | Generator QR per meja + cetak struk | ✅ Selesai | QR per meja di Admin → link `/menu/:qr`; struk di Kasir (ReceiptModal: cetak + salin) |
| 11 | Fitur lanjutan (laporan periode, stok otomatis) | ⬜ Belum | Stok otomatis belum; laporan periode ✅ (lihat step 17) |
| 12 | Admin: kelola meja & QR | ✅ Selesai | Tambah/hapus meja, atur status, QR per meja → link `/menu/:meja` |
| 13 | Admin: kelola menu lengkap + foto | ✅ Selesai | Tambah/hapus menu, ubah nama/kategori/deskripsi, foto via URL (tampil di menu pelanggan) |
| 14 | Admin: kelola staf | ✅ Selesai | Daftar user + role (tambah, ubah role, hapus) |
| 15 | Kasir: pilih meja / take-away | ✅ Selesai | Pilih meja dine-in / take-away sebelum bayar (TablePickerModal) |
| 16 | Cetak/kirim struk (Kasir) | ✅ Selesai | ReceiptModal: tampil setelah sukses bayar, tombol Cetak + Salin Struk |
| 17 | Laporan filter periode (Admin) | ✅ Selesai | Harian / 7 hari / bulan ini / semua |
| 18 | Kasir: pesanan masuk & konfirmasi | ✅ Selesai | Panel "Pesanan Masuk": order pelayan/self-order status `menunggu-konfirmasi`, tombol Konfirmasi → `diproses` (tampil di KDS), badge jumlah (polling 5s) |
| 19 | Kasir: bayar nota meja yang sudah ada | ✅ Selesai | Tab "Nota" di panel queue Kasir: daftar order `diproses` per meja, tombol Bayar (tunai/QRIS) → struk; `processPayment` → order `selesai` + meja `perlu dibersihkan` |
| 20 | Status meja otomatis | ✅ Selesai | `createOrder` dine-in → meja `terisi`; `processPayment` → `perlu dibersihkan`; admin tetap bisa ubah manual. Pelayan & Kasir refresh status meja |
| 21 | KDS urut waktu masuk | ✅ Selesai | `activeOrders` diurutkan by `createdAt` naik (tertua dulu = antrian) |
| 22 | Tes otomatis | ✅ Selesai | Vitest + React Testing Library + jest-dom (18 tes): service layer (`mockApi` login/createOrder/getSalesSummary) & guard/redirect (ProtectedRoute, login → roleHome). Jalankan `npm test` |
| 23 | Simulasi QRIS di PaymentModal | ✅ Selesai | Metode QRIS kini menampilkan QR dinamis (EMVCo, nominal total) + status "Menunggu pembayaran..." → tombol "Simulasi Pembayaran Sukses" → status "Pembayaran berhasil" → "Tandai Lunas" baru memanggil `onConfirm` |
| 24 | Fase B: setup backend infra (Laragon) | ✅ Selesai | MySQL 8.4 (`dineflow_pos`) + Redis 5.0 aktif; `.env` → mysql + predis + reverb; install Sanctum/Reverb/predis; migrate dasar + personal_access_tokens sukses. Detail di `docs/todo.md` B0 |
| 25 | Fase B: data layer backend (B1a) | ✅ Selesai | Migrasi + model + seeder meniru `mockData.ts`: users (username+role), tables, menu_categories, menu_items, orders, order_items, payments. `migrate:fresh --seed` sukses (users=4, menu=19, tables=8, orders=3, items=6, total ORD-0001=30800). User model pakai `HasApiTokens` |
| 26 | Fase B: API endpoints + controller (B1b) | ✅ Selesai | `install:api` (routes/api.php + bootstrap), CORS, 7 Resource (camelCase), 7 controller (Auth, Category, MenuItem, Table, Order, Payment, User, SalesSummary). 22 route. Uji curl: login/token, categories, menu-items, tables CRUD, orders (create+confirm+updateItemStatus), payments (ubah meja → perlu-dibersihkan), users CRUD, sales-summary, logout, 401 JSON |
| 27 | Fase B: sambung httpApi (B1c) | ✅ Selesai | `httpApi.ts` (fetch + Bearer + unwrap `{data}` + 401) menggantikan `mockApi.ts` sebagai `api` instance. AuthContext simpan token (`dineflow-token`). Proxy Vite `/api` → `127.0.0.1:8000`. Uji via browser proxy: login, orders, createOrder (meja terisi) OK. Tes guard diadaptasi (stub fetch) |
| 28 | Fase B: real-time Reverb + Redis (B2) | ✅ Selesai | Event `OrderStatusChanged` (ShouldBroadcastNow) + `broadcastAs()` wajib (tanpa itu nama event jadi full class `App\Events\...` → client tidak menerima). Channel publik `orders` (kasir/dapur/pelayan) & `order.{orderNumber}` (pelanggan). `reverb:start` port 8080. `laravel-echo`+`pusher-js` + `src/services/echo.ts` (client option). Polling 5s dihapus di KitchenPage, KasirPage, MenuPage. Verifikasi end-to-end: order via API → client terima event ≤2s |
| 29 | Fase B: auth & akses publik (B3 Opsi A) | ✅ Selesai | 2 bug fix: (1) **Akses publik** — GET `/categories`, `/menu-items`, `/tables` & POST `/orders` keluar grup `auth:sanctum` di `backend/routes/api.php`; MenuPage `/menu/:table` jalan tanpa login (verifikasi curl tanpa token: publik=200, GET /orders=401, self-order=201). (2) **Logout revoke token server** — `Api.logout()` (POST `/logout` + `clearToken`) di `httpApi.ts`, stub di `mockApi.ts`, `AuthContext.logout` panggil `api.logout().catch(()=>{})`. Verifikasi curl: login→logout→token lama=401. Build/lint/18 tes frontend + 2 tes backend lolos |
| 30 | Fase B: foto menu upload (B4) | ✅ Selesai | Upload file foto menu ke server: `MenuItemController` terima field `image` (multipart, max 2 MB) di store/update → simpan disk `public/menu-items` → `image_url` absolut `APP_URL/storage/...` (via `storage:link`). Frontend: `MenuManagement` ganti input URL → `<input type=file>` + preview; `httpApi` kirim FormData (update pakai POST `_method=PUT`). URL string lama tetap didukung. Verifikasi curl: upload→URL tersimpan & file dapat diakses (200); build/lint/18 tes frontend + 2 tes backend lolos |
| 31 | Uji manual menyeluruh + enhancement menu real-time | ✅ Selesai | Uji manual `docs/uji manual.md` §1–§7 semua ✅ (Opsi A). 2 bug ditemukan & difix saat §2.10 QRIS: (1) backend "undefined array key `cashReceived`" → `$cashReceived = $validated['cashReceived'] ?? null`; (2) halaman putih setelah Tandai Lunas karena `change: null` → `change: payment.change ?? undefined` di KasirPage + guard `!= null` di ReceiptModal. Enhancement: event `MenuChanged` (channel `menu`) → setiap perubahan menu (Habis/Tersedia, nama/harga/deskripsi/foto, tambah/hapus) real-time di `MenuPage` & panel menu `KasirPage` tanpa refresh (pola `ShouldBroadcastNow` + subscribe Echo). Verifikasi: build/lint/18 tes frontend + 3 tes backend lolos; broadcast `MenuChanged` diterima listener WS |
| 32 | Persiapan deploy (Tahap 0) | 🔄 Berjalan | Config menjadi env-driven agar satu kode jalan di lokal & production: `config/cors.php` baca `FRONTEND_URL`, `config/reverb.php` origin via `REVERB_ALLOWED_ORIGINS`, `config/filesystems.php` tambah `photo_disk` (`PHOTO_DISK`), `MenuItemController::resolveImageUrl` pakai disk `PHOTO_DISK` (lokal `public`, produksi `s3`), `.env.example` diperbarui. Install `league/flysystem-aws-s3-v3` (S3/Supabase). Commit `e0daf66` ter-push. Tes 3/3 backend + build/lint/18 tes frontend hijau. Provider produksi terpilih: **PostgreSQL→Neon**, **Redis→Upstash**, **foto→Supabase S3** (Render minta kartu utk DB). Sisa: buat Neon, Web Service API+Reverb di Render, deploy Vercel, verifikasi pasca-deploy (lihat `docs/deployment.md`) |
| 33 | Riwayat Transaksi (Kasir & Admin) | ✅ Selesai | Audit trail transaksi unlimited tanpa pagination. Backend: GET /orders eager load `payment` → frontend dapat method/cashReceived/change/paidAt (+bug fix deteksi role di POST /orders via `auth('sanctum')`). Kasir: tab pill "Pesanan Aktif \| Riwayat" di panel queue (Masuk/Nota tetap), riwayat filter Hari ini/Semua, klik baris → cetak ulang. Admin: tab navbar baru "Riwayat Transaksi" setelah Laporan Penjualan (`TransactionHistory.tsx`: filter tanggal/cari no.order/metode + real-time action=paid). `ReceiptModal` pindah ke `components/` + helper bersama `lib/receipt.ts`. Verifikasi: 9 tes backend, build/lint/18 tes FE, E2E curl payment di GET /orders |
| 34 | Pengaturan real-time + restrukturisasi struk | ✅ Selesai | Bug: ubah PPN/logo/nama di Admin tidak sampai ke struk Kasir tanpa refresh (settings load sekali di mount). Fix: event `SettingsChanged` (channel publik `settings`, pola `MenuChanged`) dispatch di `SettingController::update()` & `uploadLogo()` via helper `payload()` (bentuk = GET /settings); `KasirPage` & `TransactionHistory` subscribe (unwrap `{settings}`) + `leaveChannel`. Struk standar resto: lebar tetap ±80mm, tinggi mengikuti isi, header logo → nama di bawah logo → alamat; cetak `@page margin:0` (tanpa header/footer browser) + area 302px terpusat. Verifikasi: 9 tes backend, build/lint/18 tes FE, E2E WS |
| 35 | QRIS Statis Semi-Produksi | ✅ Selesai | Admin mengunggah gambar QRIS statis (pola uploadLogo: `POST /settings/qris`, validasi image max 2 MB, key `qris_image_url`). PaymentModal: jika QRIS ada → tampilkan gambar asli + teks instruksi + tombol "Sudah Terima Bayaran" (1 langkah, langsung `handleConfirm`); jika belum → simulasi EMVCo utuh sebagai fallback. KasirPage meneruskan `settings.qrisImageUrl` ke modal. payload() backend bertambah `qrisImageUrl`. Riset GoPay Merchant perorangan dicatat di `docs/catatan-presentasi.md` (gratis, MDR 0% ≤Rp500rb untuk mikro, verifikasi KTP 1–3 hari, pencairan instan T+0). Verifikasi: 15 tes backend, build/lint/18 tes FE |
| 36 | Menu Varians + Void Order | ✅ Selesai | Backend: tabel `menu_item_variants` (name, price, available, position), `variant_name` di order_items, model+controller+resource, seeder 4 item varian (Nasi Goreng/Mie Ayam/Es Teh/Es Jeruk), `POST /orders` terima `variantName`. Frontend: `useCart` compound key `menuItemId-variantName`, pill varian di MenuPanel/WaiterOrder/MenuPage, CartPanel tampil varian, MenuManagement modal CRUD varian, receipt tampilkan varian. Void: `PATCH /orders/{order}/void` (role admin+kasir), KasirQueuePanel tombol "Batalkan" + badge "Batal" di riwayat, TransactionHistory filter status + badge dibatalkan, event `voided`. Verifikasi: 15 tes backend, build/lint/18 tes FE |
| 37 | PPN otomatis persisten per-transaksi (PRD) | ✅ Selesai | Backend: migrasi `payments` + kolom `subtotal`/`ppn_amount`/`total`; `PaymentController::store` menghitung & menyimpan (`subtotal = round(total/(1+rate))`, `ppn = total - subtotal`); `PaymentResource` expose `subtotal`/`ppnAmount`/`total`. Frontend: tipe `Payment.*`, `lib/receipt.ts` pakai nilai tersimpan bila ada (fallback hitung ulang). Struk selalu konsisten dengan PPN saat transaksi |
| 38 | Void wajib alasan + role dapur (PRD) | ✅ Selesai | Backend: kolom `void_reason`/`voided_by` di `orders`; route `PATCH /orders/{order}/void` role `kasir,admin,dapur`; `OrderController::void` terima `reason` (wajib) + simpan + meja → kosong + event. Frontend: modal `VoidOrderModal` (alasan preset/teks, divalidasi) di Kasir & Kitchen (`OrderTicket` + tombol "Batalkan"); tampilkan alasan di riwayat Kasir. Seeder ORD-0004 punya `void_reason` |
| 39 | Level Kepedasan 0-5 (PRD) | ✅ Selesai | Backend: kolom `is_spicy` di `menu_items` + `spice_level` di `order_items`; `OrderController::store` validasi 0-5 & wajib utk menu `is_spicy`; `MenuItemController` terima `isSpicy`; resource expose `isSpicy`/`spiceLevel`; seeder M01/M02/M03/M07 pedas (M01 & M03 punya varian juga). Frontend: pilih level via pill 0-5 (`SpicePills`) untuk item pedas tanpa varian; item pedas ber-varian pilih varian (level 0) lalu atur level di keranjang; `useCart` keying `menuItemId-variantName-spiceLevel` (tiap level baris terpisah) + `setSpiceLevel` stepper 0-5; tampil level di CartPanel/WaiterOrder/MenuPage/receipt/ticket; Admin toggle "Item Pedas" di form menu + badge "Pedas". Verifikasi: 15 tes backend, build/lint/18 tes FE menyeluruh |
| 40 | Audit desain terhadap DESIGN.md + verifikasi build | ✅ Selesai | Verifikasi penuh: build ✅, lint (1 warning lama di AuthContext, bukan baru), 18/18 tes FE ✅, 15/15 tes backend ✅. **Audit token warna**: tidak ada hex hardcoded di komponen (semua via token `index.css`); seluruh 16 token DESAIN cocok; semua overlay modal sudah `bg-text-primary/40`. **Perbaikan disiplin token**: 2 badge di `KasirQueuePanel.tsx` pakai `text-white` → `text-text-on-accent` (nilai identik #FFFFFF, tapi sesuai aturan token). **Audit target sentuh (DESIGN.md ≥44px)**: tombol `+` item (40px→44px) & tombol `-`/`+` qty checkout (40px→44px) di `MenuPage.tsx` diperbaiki ke `h-11 w-11`; sisanya sudah ≥44px. **Kontras**: pakai badge 15% bg + teks solid (token status) — konsisten. **Disabled**: `Button.tsx` `disabled:opacity-40 pointer-events-none` sesuai DESIGN.md. **Pengecualian sah**: `ReceiptModal` `bg-white`/`text-black` hanya untuk area cetak struk thermal (`#print-area`); `animate-ping` (indikator "Menunggu...") di PaymentModal ~1s tapi untuk state berkelanjutan, bukan feedback transien — dianggap wajar |
| 41 | Panduan deploy Nevacloud (VPS 1GB hemat) | ✅ Selesai | Riset & keputusan hosting: pilih **Nevacloud Nevalite 1GB** (DDR4, 1 vCPU/1GB/20GB, Ubuntu 24.04, **Fixed Term** Rp42.240/bln, bayar QRIS tanpa kartu kredit) untuk backend + Reverb, dengan layanan **gratis eksternal**: Neon (PostgreSQL), Upstash (Redis), Supabase S3 (foto menu), Vercel (frontend), DuckDNS + Let's Encrypt (domain/SSL/`wss`). Keputusan: **1GB cukup untuk 1 proyek** karena DB/Redis/foto di luar VPS (RAM hanya Nginx+PHP+Reverb); upgrade 1GB→2GB via Scale Up bila nanti butuh 2 proyek. `docs/deployment.md` ditambah **§1.Opsi-D** + **§1.Opsi-D-Setup**: panduan lengkap order Nevalite, install (tanpa cPanel/DB/Redis lokal), setup `.env` ikut template produksi `.env.example`, Supervisor (Reverb), Nginx, DuckDNS+certbot, deploy Vercel, verifikasi, troubleshooting RAM 1GB (swap, `pm.max_children`, `QUEUE_CONNECTION=sync`), dan catatan upgrade 2 proyek. Tidak ada perubahan kode aplikasi (murni dokumentasi deploy) |
| 42 | Konversi mockup Stitch ke React + durasi duduk | ✅ Selesai | Menyesuaikan 4 komponen frontend agar mengikuti 4 mockup Google Stitch (Table Map, Menu Ordering, Active Orders, Customer Self-Order) — **warna tetap token `DESIGN.md`** (tanpa hex baru), **Bahasa Indonesia**, harga **Rupiah**, ikon **SVG**, **tanpa** sidebar/bottom-nav (tetap `TopNavBar`). **(1) `TableSelect`** — kartu persegi + ikon + "N Pax" + **durasi duduk** meja `terisi` (dari order aktif terawal, auto-refresh 30s). **(2) `WaiterOrder`** — kartu menu vertikal → **horizontal** + thumbnail foto item (fallback placeholder). **(3) `OrderList`** — garis status kiri berwarna, elapsed time, note item jadi badge merah, tombol "Antarkan". **(4) `MenuPage`** — grid 2 kolom → kartu horizontal + overlay "Habis". **(5) DRY** — `formatElapsed` pindah ke `lib/format.ts`. Backend: seeder `created_at` order aktif relatif `now()` agar durasi tidak puluhan jam. Verifikasi: build ✓, lint ✓, 18/18 tes FE ✓ |
| 43 | Perdalam 4 layar ke mockup Stitch (pendalaman lanjutan) | ✅ Selesai | Menambah elemen mockup yang belum teradopsi di step 42 (tetap token DESIGN.md, ikon SVG, Bahasa Indonesia, Rupiah). **(1) `TableSelect`** — nomor meja diperbesar `text-subheading`→`text-heading` (berwarna accent utk `terisi`), label status jadi **pill** (`rounded-full bg-status-*/15`) di bawah kartu. **(2) `OrderList`** — tambah **ikon jam** (SVG) di samping elapsed `Meja X · durasi`. **(3) `WaiterOrder`** — Review Order bar sticky (jumlah item + total + "Lihat & Kirim") **sudah ada**, tidak perlu diubah. **(4) `MenuPage`** — tambah **Featured Card** ("menu pertama" kategori aktif, keputusan user): gambar besar h-48 + badge harga di kanan atas + nama + deskripsi + tombol "Tambah ke Pesanan" lebar (menangani varian & item pedas via SpicePills); daftar reguler pakai `slice(1)` agar unggulan tidak duplikat. Keputusan: **skip** floor tabs & tombol Filter (aplikasi single-outlet tanpa data floor); tombol OrderList tetap satu "Antarkan" (tanpa View Details/Add Note). Verifikasi: build ✓, lint ✓, 18/18 tes FE ✓ |
| 44 | Bayar di Muka (Prepay) semua kanal + integrasi DOKU QRIS | ✅ Selesai | D1 backend ✅ (22 tes) & D2 frontend ✅ (tsc/build/lint/17 tes). Alur: semua kanal order dibuat `menunggu` (belum ke dapur) → bayar di muka (kasir: Tunai ATAU QRIS DOKU; self-order & pelayan: QRIS DOKU — pelayan tunjukkan QR di HP/tablet) → status `paid` → order `diproses` (KDS) + meja `terisi`. Tombol "Konfirmasi" kasir **dihapus** (lunas → otomatis ke dapur). Tambah endpoint/aksi `complete` (Tandai Selesai) → `selesai` + meja `perlu-dibersihkan`. `PAYMENT_DRIVER=mock` default (demo tanpa kredensial DOKU); switch ke `doku` cukup 1 env + set kredensial. D3 (deploy DOKU produksi/sandbox) belum dimulai |
| 45 | Self-order pilih metode bayar + barcode kasir (QR-URL + scan kamera) | ✅ Selesai | Frontend: self-order setelah "Lanjut ke Pembayaran" → layar **pilih metode** (QRIS langsung / **Bayar di Kasir**). Bayar di Kasir: barcode kini **URL** `${origin}/order/ORD-XXXX` (HP scan → buka tracking; alat kasir → mengandung nomor). Route publik `/order/:orderNumber` → `OrderTrackingPage` (tanpa login, real-time `order.{orderNumber}`), UI tracking diekstrak ke komponen `OrderTracking` (dipakai MenuPage + halaman baru). **Kotak scan** tab Masuk Kasir (`KasirScanBox`): input (barcode gun USB = ketik+Enter) ATAU tombol kamera (`ScanCameraModal` via lib `html5-qrcode`, ganti Belakang/Depan); ekstraksi nomor via `lib/scan.ts` (`ORD-\d{4}`, tolerir URL/teks polos) → buka modal Bayar; feedback bila tidak dikenali. Backend: **fix bug** `PaymentResource($order)` di `store` → `PaymentResource($order->payment)` (field payment sebelumnya null → struk rusak); endpoint publik `GET /order-status/{orderNumber}` (track). Verifikasi: backend 23/23 tes, FE build/lint/22 tes (5 tes scan baru). Catatan keamanan: kamera wajib HTTPS di produksi; di dev lokal pakai barcode gun/ketik manual |
| 46 | Audit konsistensi frontend menyeluruh (A1 + Status + Visual + Bahasa) | ✅ Selesai | Sesi audit opsi a+b: verifikasi konsistensi search, status/badge, token desain, bahasa, responsif; lalu perbaikan 5 batch ter-push. **(Batch 1)** Self-order dapat pesan kosong "Tidak ada menu ditemukan." saat hasil filter/search habis (`MenuPage`), pola Kasir/Pelayan. **(Batch 2)** Ekstrak semua pemetaan status ke **`lib/statusConfig.ts`** (sumber tunggal: `orderStatusBadge/Label/Bar`, `itemStatusBadge/Label`, `tableStatusLabel`) dipakai OrderList, OrderTracking, TablePickerModal — hapus duplikasi lokal; **fix bug** order `dibatalkan` di OrderTracking dulu jatuh ke `'new'` kini `'cancelled'`; `diproses` OrderList `neutral`→`cooking` seragam; label meja kasir "Bersihkan"→"Perlu Dibersihkan". **(Batch 3)** Kapitalisasi label status item & order ("baru"→"Baru") di OrderTicket, OrderTracking, OrderList (+order-level `orderStatusLabel`). **(Batch 4)** Token: overlay QRIS Pelayan `bg-black/60`+`shadow-xl`→`bg-text-primary/40`+`shadow-modal`; `font-num` harga varian menu (MenuManagement, MenuPage ×2, WaiterOrder). **(Batch 5)** Terjemah Indonesia: Bawa Pulang (4), Tahan, Keranjang, Ubah, Ekspor, Atur Ulang, Pesan Mandiri, No. Pesanan, mis., peran, Masuk. Verifikasi: FE lint ✓ (hanya warning lama), build/typecheck ✓, **24/24** tes FE tiap batch |

## 2. Cara Kerja (konvensi wajib)

- **Bertahap**: kerjakan satu fitur/step per batch, bukan semuanya sekaligus.
- **Verifikasi wajib** tiap selesai: `npm run build` (tsc), `npm run lint` (oxlint), lalu `npm test` (vitest) di folder `frontend/`.
- **Warna wajib dari token DESIGN.md** (`src/index.css`) — jangan tambah hex baru di luar tabel token. Aksen biru `#2563EB` hanya untuk elemen aksi utama.
- **Angka/harga/kode order** pakai font monospace (`font-num`).
- **Semua data lewat service layer** (`api.*` dari `src/services/api.ts`) — komponen UI tidak boleh memanggil mock/fetch langsung, supaya gampang ditukar ke backend Laravel.
- **Race condition / transaksi**: aturan AGENTS.md = database transaction + `lockForUpdate()` di backend Laravel, BUKAN Golang/Kafka.
- **Real-time**: Laravel Echo + Reverb + Redis, BUKAN websocket/Node terpisah.
- **Mockup Stitch** (`resto-pos-frontendstitch-export-*`) = panduan LAYOUT; warna/komponen mengikuti DESIGN.md (keputusan: putih/bersih, bukan gaya industrial).

## 3. Peta File (`frontend/src/`)

| File | Fungsi |
|---|---|
| `App.tsx` | Router: `/` → `/kasir`, plus `/kitchen`, `/pelayan`, `/menu/:table`, `/admin`, `/login`, `*` |
| `main.tsx` | Entry point React |
| `index.css` | Token Tailwind v4 (`@theme`) dari DESIGN.md: warna, font, radius, shadow |
| `types/index.ts` | Tipe data PRD: User, DiningTable, MenuCategory, MenuItem, Order, OrderItem, Payment, SalesSummary, SalesPeriod |
| `services/api.ts` | **Kontrak API** (`Api` interface) + payload types — target implementasi Laravel |
| `services/mockData.ts` | Data tiruan (menu, meja, order awal) + `TAX_RATE` (10%) |
| `services/mockApi.ts` | Implementasi `Api` in-memory (state module-level agar lintas halaman berbagi data) — kini hanya dipakai tes service layer |
| `services/httpApi.ts` | **Implementasi `Api` aktif** memakai fetch ke Laravel (unwrap `{data}`, Bearer token, 401) |
| `services/echo.ts` | **Klien real-time** Laravel Echo + Reverb (protokol Pusher); subscribe channel `orders` & `order.{orderNumber}`. Wajib `cluster:'mt1'` (agar pusher-js tidak error) & `namespace:''` (agar nama event cocok dengan `broadcastAs()` server) |
| `services/mockApi.test.ts` | Tes service layer: login, createOrder (prepay `menunggu` + pajak), QRIS checkout+mock-paid → `diproses` + meja `terisi`, processPayment tunai prepay, getSalesSummary |
| `context/AuthContext.tsx` | Sesi login (mock), simpan user aktif di localStorage |
| `hooks/useCart.ts` | State keranjang bersama (Kasir, Pelayan, Menu QR): add(increment)/decrement/note/clear + summary; key compound `menuItemId-variantName` untuk dukung varian |
| `lib/format.ts` | `formatRupiah()` |
| `lib/receipt.ts` | `orderToReceipt(order, payment, settings)` — bangun data struk dari order; dipakai Kasir (bayar & cetak ulang) dan Admin |
| `lib/statusConfig.ts` | **Sumber tunggal** pemetaan status (Sesi Audit): `orderStatusBadge/Label/Bar`, `itemStatusBadge/Label`, `tableStatusLabel` — dipakai OrderList, OrderTracking, OrderTicket, TablePickerModal agar konsisten & anti-drift |
| `lib/roles.ts` | Label & aturan role (`roleLabel`, role yang boleh akses tiap halaman) |
| `components/Button.tsx` | Tombol primary/outline/danger, ukuran sm/md/lg |
| `components/QrisPay.tsx` | Komponen QRIS bayar di muka (self-order, pelayan, kasir): tampil QR + polling `getPaymentStatus` tiap 3s + tombol "Saya Sudah Bayar (Demo)" bila driver `mock`; bila `paid` → `onPaid` |
| `components/Card.tsx` | Kartu dasar (radius 12, shadow level 1) |
| `components/StatusBadge.tsx` | Badge pill status (new/cooking/ready/done/danger/neutral/cancelled), bg 15% + teks warna solid |
| `components/CategoryTabs.tsx` | Tab kategori menu (aktif = biru) |
| `components/TopNavBar.tsx` | Navbar atas (Kasir/Admin): logo + link halaman + indikator online |
| `components/ProtectedRoute.tsx` | Guard rute: cek login + role, redirect ke `/login` |
| `components/guard.test.tsx` | Tes guard & redirect: ProtectedRoute (login/role), LoginPage → `roleHome`, mapping `roleHome` |
| `components/HomeRedirect.tsx` | Redirect `/` sesuai role user (login → dashboard role, belum → `/login`) |
| `components/ReceiptModal.tsx` | Struk digital (dipakai Kasir & Admin): cetak printer + salin teks struk — dipindah dari `pages/kasir/` saat fitur riwayat; kini gaya thermal 80mm (lebar tetap, tinggi mengikuti item), header logo → nama di bawah logo → alamat, cetak terpusat tanpa header/footer browser |
| `pages/PlaceholderPage.tsx` | Halaman kosong untuk rute placeholder/404 |
| `pages/auth/LoginPage.tsx` | Halaman login mock (username = role, password 1234) |
| `pages/kasir/KasirPage.tsx` | Orkestrasi Kasir (load data, filter, bayar di muka cart/nota → tunai/QRIS DOKU, struk, Tandai Selesai); subscribe channel `orders` (order & meja) + `menu` (reload menu saat berubah) + `settings` (PPN/logo/nama live) |
| `pages/kasir/KasirQueuePanel.tsx` | Panel queue Kasir ber-tab pill "Pesanan Aktif" (sub-tab "Masuk" order `menunggu` + tombol "Bayar di Muka" + Batalkan & "Nota" order `diproses` + badge "Lunas" + "Tandai Selesai" + Batalkan) dan "Riwayat" (order `diproses`/`selesai`/`dibatalkan`, filter Hari ini/Semua, badge Batal untuk dibatalkan, klik baris → cetak ulang), badge jumlah |
| `pages/kasir/MenuPanel.tsx` | Kolom kiri Kasir: kategori + cari + daftar menu (pill varian Reguler/Besar untuk item yang punya varian) |
| `pages/kasir/CartPanel.tsx` | Kolom kanan Kasir: keranjang nota (tampil varian di samping nama item) + subtotal/pajak/total + Hold/Bayar di Muka |
| `pages/kasir/PaymentModal.tsx` | Modal bayar di muka kasir: Tunai (input uang + kembalian) / QRIS DOKU dinamis (`checkoutOrder` → `QrisPay`: polling status + tombol demo for driver `mock`) |
| `pages/kasir/TablePickerModal.tsx` | Pilih meja / take-away sebelum bayar (status per meja) |
| `pages/pelayan/PelayanPage.tsx` | Orkestrasi Pelayan: view tables → order → orders, dengan `TopNavBar` (logout/navigasi); setelah submit order muncul overlay `QrisPay` (QRIS DOKU bayar di muka, QR ditunjukkan ke pelanggan) |
| `pages/pelayan/TableSelect.tsx` | Peta meja (status Kosong/Terisi/Perlu Dibersihkan) |
| `pages/pelayan/WaiterOrder.tsx` | Input pesanan mobile + bottom sheet keranjang + kirim → bayar QRIS di muka |
| `pages/pelayan/OrderList.tsx` | Daftar pesanan + tandai diantar |
| `pages/kitchen/KitchenPage.tsx` | KDS: grid ticket (hanya order `diproses` = sudah lunas), dengan `TopNavBar` (logout/navigasi) |
| `pages/kitchen/OrderTicket.tsx` | Kartu ticket besar, border kiri warna status, tombol Mulai Masak/Siap Saji; tampilkan varian di samping nama item |
| `pages/menu/MenuPage.tsx` | Pesan mandiri publik: katalog (pill varian), keranjang ("Bayar di Muka"), view pembayaran (`QrisPay`) → tracking, validasi meja; subscribe channel `menu` (reload katalog saat menu berubah) & `order.{orderNumber}` (tracking) |
| `pages/admin/AdminPage.tsx` | Tab Manajemen Menu / Meja / Staf / Laporan Penjualan |
| `pages/admin/MenuManagement.tsx` | Kelola menu: tambah/hapus, edit nama/kategori/deskripsi/foto, ubah harga, tandai tersedia/habis; modal edit kini termasuk CRUD varian (nama, harga, aktif) |
| `pages/admin/TableManagement.tsx` | Kelola meja: tambah/hapus, atur status, lihat QR per meja |
| `pages/admin/StaffManagement.tsx` | Kelola staf: tambah, ubah role, hapus |
| `pages/admin/SalesReport.tsx` | Laporan: kartu total/transaksi/rata-rata + menu terlaris, filter periode (harian/7 hari/bulan ini/semua) |
| `pages/admin/TransactionHistory.tsx` | Riwayat transaksi: daftar order `diproses`/`selesai`/`dibatalkan` (sudah bayar; `menunggu` belum lunas tidak tampil) — tanggal+jam, no.order, meja/sumber, item, total, metode/status, filter rentang tanggal + cari no.order + dropdown metode + dropdown status, klik baris → cetak ulang struk; real-time subscribe `orders` action=`paid`/`voided` + `settings` |

Catatan: `mockApi.ts` memakai state module-level (`orders`, `menuItems`) sehingga pesanan baru dari satu halaman ikut terlihat di halaman lain dalam 1 sesi browser. Saat pindah ke Laravel, ini menjadi data server.

## 4. Rencana Langkah Berikutnya (urutan saran)

Urutan dibagi dua: **A) fitur frontend murni (tidak butuh backend, bisa dikerjakan sekarang)** dan **B) tergantung backend Laravel**.

### A. Frontend murni (bisa dikerjakan sekarang)

1. ✅ **Login & guard role**: halaman login, guard per role, protect `/kasir`, `/kitchen`, `/pelayan`, `/admin`. `/menu/:table` publik. Akun demo (username = role, password `1234`), sesi tersimpan di localStorage.
2. ✅ **Admin: kelola meja & QR**: daftar meja, tambah/hapus, atur status, tampilkan QR per meja (link ke `/menu/:meja`).
3. ✅ **Admin: kelola menu lengkap**: tambah/hapus menu, ubah nama/kategori/deskripsi, foto via URL (tampil di menu pelanggan sesuai DESIGN.md).
4. ✅ **Admin: kelola staf**: daftar user + tambah + ubah role + hapus.
5. ✅ **Kasir: pilih meja / take-away**: pilih meja saat dine-in di kasir (sekarang `tableId` terisi sesuai pilihan).
6. ✅ **Cetak/kirim struk**: struk digital (`ReceiptModal`) dengan tombol Cetak + Salin, muncul setelah bayar berhasil.
7. ✅ **Laporan filter periode**: harian / 7 hari / bulan ini / semua (`SalesReport`).
8. ✅ **Tes otomatis (A5)**: Vitest + React Testing Library + jest-dom; 18 tes (service layer + guard/redirect). Jalankan `npm test`.

> ✅ **Fase A (frontend murni) SELESAI.** Semua fitur fase A selesai; lanjut ke fase B (backend Laravel) — lihat bagian B di bawah.

### B. Tergantung backend Laravel

1. **API Laravel**: implement `Api` interface dengan fetch/axios ke endpoint backend (base URL di `.env`). Setup CORS backend. ✅ **Selesai (B1b + B1c)**.
2. **Real-time Reverb**: ganti polling 5s di `KitchenPage` & `MenuPage` (tracking) dengan `laravel-echo` subscribe ke channel pesanan. NFR: notifikasi ≤ 2 detik. ✅ **Selesai (B2)** — juga panel Kasir.
3. **Auth nyata**: ganti mock login dengan endpoint auth Laravel (Sanctum) + role dari server. ⬜ Belum (B3).

## 5. Log Keputusan

- **Bayar di Muka (Prepay) — keputusan user**: semua kanal (self-order, kasir, pelayan) WAJIB bayar di muka sebelum dapur memasak. Semua order dibuat `menunggu` (tidak masuk KDS); pindah ke `diproses` + meja `terisi` HANYA setelah pembayaran `paid`. Kanal kasir pilih **Tunai ATAU QRIS DOKU**; self-order & pelayan **QRIS DOKU** saja (pelayan tunjukkan QR di HP/tablet ke pelanggan). Tombol "Konfirmasi" kasir DIHAPUS (lunas otomatis ke dapur). `selesai` kini dicapai lewat aksi baru "Tandai Selesai" (`complete`) karena pembayaran sudah di muka. Vendor: **DOKU** (QRIS, sandbox untuk demo PKL) lewat interface `PaymentGateway` (`DokuGateway` + `MockQrisGateway`), switch lewat `PAYMENT_DRIVER` di `.env` — default `mock` agar berfungsi penuh tanpa kredensial; ke `doku` cukup 1 env + set key.
- **Desain**: layout ikuti mockup Stitch, tetapi warna/komponen wajib dari DESIGN.md (putih + aksen biru `#2563EB`) — bukan gaya industrial mockup (diputuskan user).
- **Struktur app**: satu SPA React dengan routing, bukan 4 aplikasi terpisah (user pilih rekomendasi).
- **Sumber data**: mock dulu via `MockApi`, service layer siap ditukar ke Laravel (user pilih).
- **Lokasi kode**: dibangun di folder `frontend/` di dalam repo ini, nanti digabung ke folder utama `resto-pos/` bersama `backend/`.
- **PRD asli**: file `_ PRODUCT REQUIREMENTS DOCUMENT (PRD).md` adalah dokumen Word (.docx) ber-ekstensi `.md` — isi sudah dibaca & disarikan di sini.
- **Cetak struk (Kasir)**: setelah bayar sukses langsung tampil `ReceiptModal` (bukan modal sukses statis). Tombol Cetak pakai `window.print()` dengan area `#print-area` khusus; tombol Salin Struk menyalin teks struk ke clipboard.
- **Filter laporan**: `Api.getSalesSummary(period)` menerima `SalesPeriod` (`harian`/`mingguan`/`bulanan`/`semua`). Implementasi mock memfilter `order.createdAt`; saat ke Laravel cukup ganti jadi query tanggal di backend.
- **Foto menu**: pakai URL gambar (belum upload file). Ketika backend siap, ganti ke upload + penyimpanan URL dari server (lihat catatan step 13).
- **Navbar di semua halaman role**: `TopNavBar` kini dipakai di Kasir, Admin, Pelayan, dan Kitchen (sebelumnya Pelayan & Kitchen tampil full-screen tanpa logout/navigasi → user "terjebak"). Setiap role melihat link sesuai hak aksesnya + tombol Keluar; admin melihat semua link (Kasir/Dapur/Pelayan/Admin). Akses antar role tetap diatur `ProtectedRoute` (mis. pelayan tidak bisa ke `/admin`).
- **Alur pesanan masuk Kasir (A1)**: ditambah status order `menunggu-konfirmasi`. `createOrder` dari source `pelayan`/`self-order` → `menunggu-konfirmasi` (belum tampil di dapur); Kasir mengonfirmasi lewat `api.confirmOrder(orderId)` → `diproses` (aktif di KDS). KDS kini hanya menampilkan `diproses` (bukan semua kecuali selesai/dibatalkan). Panel "Pesanan Masuk" di Kasir memakai polling 5s (sama seperti KDS) sebagai simulasi real-time; saat fase B polling diganti Laravel Echo.
- **Bayar nota meja (A2)**: panel queue Kasir diubah jadi ber-tab "Masuk"/"Nota" (`KasirQueuePanel`, menggantikan `PendingOrdersPanel`). Tab "Nota" menampilkan order `diproses` per meja → tombol Bayar → `PaymentModal` (total dari `order.total`) → struk. `processPayment` kini juga menandai meja order dine-in jadi `perlu dibersihkan`.
- **Konsistensi total pesanan**: `createOrder` kini menghitung `order.total` SUDAH termasuk pajak 10% (`Math.round(subtotal * 1.1)`), sama seperti `cart.summary.total`. Total mock order 1 & 2 diperbarui (30800 & 39600) dan ditambah mock order 3 status `menunggu-konfirmasi` (demo panel "Masuk"). Sebelumnya `order.total` = subtotal tanpa pajak (tidak konsisten dengan tagihan yang dibayar).
- **Status meja otomatis (A3)**: `createOrder` dengan `tableId` langsung menandai meja `terisi` di `mockApi`; `processPayment` menandai `perlu dibersihkan` (sudah di A2). Admin tetap bisa ubah manual. `PelayanPage` & `KasirPage` kini me-refresh status meja (setelah submit order / ikut polling 5s) agar peta meja & pemilih meja akurat.
- **Tes otomatis (A5)**: setup Vitest 4 (jsdom) via `vitest.config.ts` + `src/test/setup.ts` (import jest-dom/vitest, `afterEach` cleanup + clear localStorage). Skrip `npm test` (sekali jalan) & `npm run test:watch`. `globals: true` agar auto-cleanup RTL jalan. Konfigurasi test terpisah dari `vite.config.ts` (plugin react + alias `@` saja, tanpa tailwind). Tes mencakup service layer (`mockApi`: login, createOrder status+pajak, confirmOrder, processPayment+meja, getSalesSummary) dan guard (`ProtectedRoute` login/role, LoginPage → `roleHome`, mapping `roleHome`).
- **Sesi login per-tab (pasca uji manual)**: Uji manual multi-tab (kasir + dapur + admin dalam 4 tab di origin sama) gagal saat bayar nota — error 403 "Akses ditolak". Akar masalah: token (`dineflow-token`) & user (`dineflow-user`) disimpan di `localStorage` yang **dibagi semua tab**; login role lain di tab lain menimpa token, sehingga request tab kasir terkirim dengan token dapur → ditolak `EnsureRole`. **Solusi**: pindahkan penyimpanan sesi dari `localStorage` ke `sessionStorage` (per-tab) di `httpApi.ts` (TOKEN_KEY/USER_KEY getter/setter/clear) & `AuthContext.tsx` (STORAGE_KEY) + sinkronkan mock tes `guard.test.tsx` (`seedUser`) & `test/setup.ts` (`sessionStorage.clear()`). Database tidak tersentuh. Build/lint/18 tes frontend lolos. Efek: tiap tab punya sesi sendiri; login ulang 1x per tab setelah fix.
- **Laporan Penjualan real-time (pasca uji manual)**: `SalesReport` sebelumnya hanya fetch sekali per periode (`useEffect [period]`), tanpa subscribe — laporan admin tidak berubah saat kasir membayar. **Solusi**: subscribe `echo.channel('orders')` mendengar `OrderStatusChanged` dengan `action === 'paid'` → refetch `api.getSalesSummary(period)` tanpa flash loading (parameter `loadSummary(showLoading)`; loading hanya saat ganti periode). `leaveChannel` saat tab laporan ditutup.
- **Cart Menu QR konsisten mobile (pasca uji manual)**: view `cart` ("Lihat Pesanan") di `MenuPage` memakai `fixed inset-0` full-screen → membentang selebar layar desktop, tidak konsisten dengan view menu & tracking (`max-w-md`). **Solusi**: bungkus ulang jadi overlay gelap `bg-slate-900/40` + panel tengah `w-full max-w-md` (pola sama `PaymentModal`). Menu, keranjang, dan "Pesanan Terkirim" kini sama-sama lebar mobile.
- **Simulasi QRIS (Kasir)**: `PaymentModal` metode QRIS menampilkan QR dinamis EMVCo (fungsi lokal `buildQrisPayload` + CRC16-CCITT di `PaymentModal.tsx`) berisi nominal total, bukan teks statis. Alur simulasi: "Menunggu pembayaran..." (animasi ping) → klik "Simulasi Pembayaran Sukses" → status "Pembayaran berhasil" → klik "Tandai Lunas" baru memanggil `onConfirm({ method: 'qris' })`. Merchant fiktif `ID.CO.DINEFLOW.QRIS`; saat fase B QR asli dari penyedia payment gateway.
- **Fase B - setup infrastruktur (B0)**: Laragon aktif di `D:/laragon/` (bukan `C:/laragon/` yang hanya berisi php). MySQL 8.4.3 jalan via GUI Start All (data dir `D:/laragon/data/mysql-8.4`); Redis 5.0.14 tidak jalan via Start All → dijalankan manual `redis-server.exe redis.windows.conf` via `Start-Process` (Windows tidak mendukung `--daemonize`, proses menempel ke shell jika langsung). Database `dineflow_pos` dibuat utf8mb4. `REDIS_CLIENT=predis` dipilih karena DLL phpredis tidak ada di `D:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/ext/`. Composer harus dijalankan dengan PHP 8.3 Laragon (`D:/laragon/bin/php/php-8.3.30-Win32-vs16-x64/php.exe`) bukan XAMPP 8.2 (Laravel 13 butuh PHP ^8.3). Key Reverb: server config (`REVERB_SERVER_*`) + app credentials (`REVERB_APP_ID/KEY/SECRET`) + client (`VITE_REVERB_*`) — semua di `.env` backend.
- **Fase B - data layer (B1a)**: migrasi `users` diubah (tambah `username` unique + `role` default `kasir`; `email` → nullable) karena frontend login pakai `username` bukan email. `User` model: `HasApiTokens` + fillable baru. Entitas domain dibuat sesuai `architecture.md` §4.2 & `mockData.ts`: `tables` (number unique, seats, status, qr_code), `menu_categories` (name, order), `menu_items` (code unique, name, description, price unsigned, category_id FK, available, image_url), `orders` (order_number unique, table_id FK nullable, source, status, total), `order_items` (order_id FK cascade, menu_item_id FK nullable, name, price, quantity, note, status), `payments` (order_id FK cascade, method, amount, cash_received, change, paid_by FK users, paid_at). Model memakai atribut `#[Fillable]` (gaya Laravel 13, sama seperti `User`). Seeder `DatabaseSeeder` meniru `mockData.ts`; total order sudah termasuk pajak 10% (round). Tidak ada payment di seeder (payment dibuat saat `processPayment`, konsisten dengan mock).
- **Fase B - API endpoints (B1b)**: `php artisan install:api` gagal sebagian karena memakai PHP 8.2 XAMPP (dari PATH) tapi tetap berhasil membuat `routes/api.php` + `api:` di `bootstrap/app.php` (tidak ada migrasi duplikat — migrasi sanctum sudah ada). Composer untuk paket lain tetap pakai PHP 8.3 Laragon. CORS: Laravel 13 tidak punya tag publish cors → salin default `vendor/laravel/framework/config/cors.php` ke `config/cors.php`. Response JSON memakai API Resources (`app/Http/Resources/*`) yang mengubah snake_case DB → camelCase frontend (`categoryId`, `createdAt`, `qrCode`, dll). `install:api` menambahkan rute `/api/user` bawaan → dihapus & diganti kontrak `api.ts`. Endpoint order `confirm` = PATCH `/api/orders/{order}/confirm` (route binding model di parameter `{order}`); `updateItemStatus` = PATCH `/api/orders/{order}/items/{itemId}` (itemId diambil manual, `findOrFail`). `processPayment` transaction + `lockForUpdate`, menandai order `selesai` & meja `perlu-dibersihkan`; validasi `method` in tunai/qris. Bug fix: `$request` tidak di-pass ke closure transaction di `PaymentController`; `topItems` sales-summary kehilangan nama karena `usort` me-reindex array (diperbaiki dengan build array item dulu, lalu sort). Unauthorized API: `redirectGuestsTo(fn () => null)` di `bootstrap/app.php` agar return 401 JSON, bukan redirect ke `route('login')` yang tidak ada.
- **Fase B - sambung httpApi (B1c)**: `httpApi.ts` menjadi implementasi `Api` aktif (semua halaman + AuthContext import `@/services/httpApi`). `mockApi.ts` dipertahankan utuh karena `mockApi.test.ts` menguji MockApi in-memory (tanpa jaringan). Fitur httpApi: `BASE_URL = import.meta.env.VITE_API_URL ?? '/api'`, helper `request()` menambah header `Accept: application/json` + `Content-Type` (bila ada body) + `Authorization: Bearer <token>`; respons non-ok → ambil pesan error (dari `errors[0]` validasi atau `message`) lalu `throw new Error`; status 401 → hapus token. Response Laravel dibungkus `{ data }` (API Resource) → di-unwrap di tiap method sehingga UI menerima bentuk tipe `types/index.ts` persis seperti mock. Token disimpan di localStorage key `dineflow-token`; `AuthContext.login` kini memanggil `api.login` (yang menyimpan token internal) lalu menyimpan user; `logout` memanggil `clearToken()` + hapus user. Proxy dev di `vite.config.ts`: `/api` → `http://127.0.0.1:8000` (changeOrigin). Catatan lingkungan: Vite lama (tanpa proxy) harus di-kill dulu — port 5173/5174 tersisa dari sesi sebelumnya. Tes guard (`guard.test.tsx`) yang menguji alur login kini memakai stub `fetch` global (`vi.stubGlobal`) karena login tidak lagi in-memory.
- **Fase B - real-time Reverb + Redis (B2)**: Channel **publik** (bukan private) untuk prototipe 1 outlet — kasir/dapur/pelayan subscribe `orders`, pelanggan subscribe `order.{orderNumber}`; auth private channel (Echo + endpoint `/broadcasting/auth` + Bearer) ditangguhkan ke fase berikutnya bila butuh keamanan lebih. Event `OrderStatusChanged` memakai `ShouldBroadcastNow` (sinkron, tanpa queue worker) dan **WAJIB `broadcastAs()`** — Laravel 13 versi ini tidak lagi menyediakan default `broadcastAs` dari trait `InteractsWithSockets`, jadi tanpa itu event dikirim dengan nama full class `App\Events\OrderStatusChanged` dan client (yang bind `OrderStatusChanged`) tidak pernah menerimanya (butuh debugging panjang; solusi: definisikan `broadcastAs()`). Payload event = `{ action, order, tableStatus }`; frontend yang menerima event cukup me-reload data (`api.getOrders()` / `api.getTables()`), jadi UI tidak berubah. `MenuPage` tracking kini memakai state awal dari hasil `createOrder` + subscribe `order.{orderNumber}` — sekaligus memperbaiki bug tracking yang butuh auth (`api.getOrders` 401 untuk pelanggan tanpa token). Echo diinisialisasi via `client` option (`import Pusher from 'pusher-js'`), bukan global `window.Pusher`; `enabledTransports: ['ws','wss']` agar dev di HTTP tetap connect. Env frontend: `VITE_REVERB_APP_KEY/HOST/PORT/SCHEME` (nilai sama dengan `.env` backend).
- **Fase B - bug fix real-time (B2 lanjutan)**: Uji 2 browser menemukan Kasir→Konfirmasi→KDS masih butuh refresh manual meski code server & client benar. Isolasi berlapis: (1) listener Node connect & subscribe `orders` OK; (2) trigger event via API → order dibuat tapi listener tak menerima; (3) cek log Reverb (`reverb:start --debug`) → "Broadcasting To .. orders" muncul, berarti Reverb menerima & mem-broadcast; (4) WebSocket mentah (tanpa library) menerima event → rantai Laravel→Reverb→WS sehat; (5) `pusher-js` langsung menerima, tapi `laravel-echo` tidak → masalah di layer Echo. **Akar masalah**: laravel-echo v2.4 default `namespace: "App.Events"` (lihat `eventFormatter.format` di `echo.js`) — `echo.channel('orders').listen('OrderStatusChanged')` dibind sebagai `App.Events\OrderStatusChanged`, tidak cocok dengan nama event `OrderStatusChanged` dari `broadcastAs()` server. **Solusi**: tambah `namespace: ''` di opsi `new Echo()` di `echo.ts`. Setelah fix: verifikasi Node menerima event ≤2s + uji 2 browser (Kasir Konfirmasi → KDS tampil tanpa refresh) LOLOS. Build/lint/test frontend (18 tes) + `php artisan test` backend (2 tes) semua lolos. DB di-reset ke data demo (`migrate:fresh --seed`: 4 user, 19 menu, 8 meja, 3 order).
- **Fase B - auth & akses publik (B3 Opsi A)**: Dua bug yang tersisa setelah B1c/B2: (1) `MenuPage` publik kena 401 karena rute katalog & create order masih di dalam grup `auth:sanctum`. **Solusi**: pindahkan GET `/categories`, GET `/menu-items`, GET `/tables`, dan POST `/orders` ke luar grup auth di `backend/routes/api.php` (keputusan user: kategori+menu+meja & order self-order publik; GET `/orders`, konfirmasi, pembayaran, users, sales-summary tetap auth). `OrderController::store` tidak memakai `auth()->user()`, jadi aman jadi publik. (2) Logout hanya membersihkan token di localStorage, token Sanctum di server tidak di-revoke. **Solusi**: tambah `logout(): Promise<void>` ke kontrak `Api` (`services/api.ts`); implementasi di `httpApi.ts` = POST `/logout` + `clearToken()` di `finally` (token lokal selalu bersih walau request gagal); stub no-op di `mockApi.ts`; `AuthContext.logout` memanggil `api.logout().catch(() => {})` (best-effort, tetap hapus sesi lokal). Verifikasi curl: tanpa token katalog/menu/meja=200 & self-order=201, GET `/orders`=401; login→logout→token lama dipakai GET `/orders`=401. Build/lint/18 tes frontend + 2 tes backend lolos. DB di-reset ulang ke demo (`migrate:fresh --seed`).
- **Fase B - foto menu upload (B4)**: Ganti input URL gambar di `MenuManagement` menjadi upload file. Backend: `MenuItemController.store/update` menerima field file `image` (validasi `image, max:2048`), disimpan via `$request->file('image')->store('menu-items', 'public')`, lalu `image_url` diisi `Storage::disk('public')->url($path)` (absolut `APP_URL/storage/...`). `php artisan storage:link` membuat `public/storage` → `storage/app/public`. Field string `imageUrl` lama tetap didukung (untuk kompatibilitas & menghapus foto dengan nilai kosong). **Penting (method spoofing)**: PHP hanya mengisi `$_POST`/`$_FILES` untuk multipart **POST** — multipart PUT (curl `-X PUT -F`, dan juga `fetch(url, {method:'PUT', body: FormData})`) TIDAK mengisi file sehingga update file diam-diam tidak berfungsi. Solusi: frontend mengirim multipart via **POST + field `_method=PUT`** di FormData (Laravel method spoofing); path tanpa file tetap PUT JSON. `httpApi` helper `appendScalar` untuk isian FormData. Frontend `MenuFormData.image?: File`, `MenuManagement` memakai `<input type=file>` + preview (`URL.createObjectURL`); `AdminPage` meneruskan `image`. Verifikasi curl: login→upload→`imageUrl` tersimpan & file `GET /storage/...` = 200; update ganti foto dengan `_method=PUT` berhasil. Build/lint/18 tes frontend + 2 tes backend lolos. DB di-reset ulang ke demo.
- **Fase B - finalisasi B5 & perapian docs**: `OrderController::store` (transaction + `lockForUpdate()` pada tabel & menu item, cek ketersediaan) dan `PaymentController::store` (transaction + `lockForUpdate()` pada order, cegah bayar ganda → 409) — race condition sudah sesuai aturan AGENTS.md (BUKAN Golang/Kafka). Ditambah tes fitur backend `OrderPaymentTest` (pembayaran dua kali → 409); total tes backend jadi 3. Ringkasan status di `docs/todo.md` dirapikan (Fase A/B/GitHub semua ✅) dan `docs/uji manual.md` diperbarui ke kondisi sekarang: real-time Reverb (bukan polling 5s), upload foto menu, self-order publik tanpa login (buka `/menu/T1` incognito), dan data tersimpan di MySQL (bukan memori browser).
- **Uji manual menyeluruh + 2 bug fix (sesi uji manual)**: Semua kasus `docs/uji manual.md` §1–§7 diuji manual (Opsi A) & ditandai ✅. Bagian 4 (KDS real-time) sempat gagal di browser karena tab lama; dipastikan dengan listener WS mentah bahwa rantai Laravel→Reverb→WS sehat (event `OrderStatusChanged` diterima) — masalah murni di sisi tab browser (hard refresh) → real-time terbukti ≤2s. Dua bug nyata ditemukan di §2.10 (QRIS): (1) `PaymentController` memakai `$validated['cashReceived'] !== null` padahal QRIS tidak mengirim `cashReceived` → "Undefined array key cashReceived" (500); fix `$cashReceived = $validated['cashReceived'] ?? null`. (2) Backend mengembalikan `change: null` untuk QRIS → `ReceiptModal` yang cek `change !== undefined` meneruskan `null` ke `formatRupiah` → crash halaman putih; fix `change: payment.change ?? undefined` (KasirPage, 2 tempat) + guard `change != null` (ReceiptModal, 2 tempat). §7.6 diverifikasi via curl: `/api/me` 200 → logout 200 → token lama 401. Semua lolos; DB di-reset ke demo.
- **Enhancement menu real-time (pasca uji manual)**: Atas pertanyaan user apakah halaman Menu QR perlu refresh saat menu "Habis", diputuskan menambah real-time (sarankan: sesuai PRD "otomatis tidak muncul lagi" & "instan"), lalu diperluas agar **semua** perubahan menu real-time. Backend: event `App\Events\MenuChanged` (pola `OrderStatusChanged`: `ShouldBroadcastNow` + `broadcastAs()`), broadcast di channel publik `menu`, payload = `MenuItemResource`; dispatch di `MenuItemController::store` (menu baru), `update` (semua field: nama/harga/deskripsi/kategori/available/foto), & `destroy`. Frontend: `MenuPage` & `KasirPage` subscribe `echo.channel('menu').listen('MenuChanged', reload)` (re-fetch `api.getMenuItems`) + `leaveChannel` saat unmount — jadi katalog Menu QR pelanggan **dan** daftar menu Kasir ikut ter-update. Verifikasi: listener WS mentah subscribe `menu` menerima event setelah ubah harga & toggle available via curl; build/lint/18 tes frontend + 3 tes backend lolos; DB di-reset ke demo. Gratis (infra Reverb/Redis sudah ada) & aman (kanal publik, aditif, payload data publik).
- **Persiapan deploy (Tahap 0)**: Atas kebutuhan deployment ke Vercel+Render, config dijadikan **env-driven** agar satu kode jalan di lokal (MySQL/`public`) & production (PostgreSQL/`s3`) tanpa ubah kode. Detail: `cors.php` `allowed_origins` baca `FRONTEND_URL` (default `*`); `reverb.php` `allowed_origins` baca `REVERB_ALLOWED_ORIGINS`; `filesystems.php` tambah `photo_disk` dari env `PHOTO_DISK` (default `public`); `MenuItemController::resolveImageUrl` pakai disk `config('filesystems.photo_disk')` sehingga lokal tetap `storage:link` & produksi upload ke Supabase via `s3`. Install `league/flysystem-aws-s3-v3` (Supabase/R2/S3 compatible). `.env.example` diperbarui (FRONTEND_URL, PHOTO_DISK, REVERB_ALLOWED_ORIGINS, AWS_URL, AWS_ENDPOINT, REVERB_*). **Provider produksi** (keputusan sesi deploy): PostgreSQL via **Neon** (Render minta kartu), Redis via **Upstash** (`REDIS_URL=tls://...` + `REDIS_CLIENT=predis`), foto menu via **Supabase Storage** (bucket publik `menu-photos`, endpoint `/storage/v1/s3`, `AWS_USE_PATH_STYLE_ENDPOINT=true`), frontend di **Vercel**. Supabase free pause setelah 7 hari idle → resume manual sebelum demo. Verifikasi: tes backend 3/3, build/lint/18 tes frontend hijau; commit `e0daf66` ter-push. Menunggu: buat Neon, Web Service API+Reverb di Render, deploy Vercel, verifikasi pasca-deploy.
- **Polish desain (audit token)**: Audit UI menyeluruh terhadap DESIGN.md menemukan skor 9/10 (status warna sempurna di semua antarmuka) dengan beberapa pelanggaran token yang dirapikan: (1) **overlay modal** `bg-slate-900/40` → `bg-text-primary/40` di 7 file modal (`PaymentModal`, `ReceiptModal`, `TablePickerModal`, `MenuPage` cart, `MenuManagement`, `TableManagement` QR, `ChangePasswordModal`) — nilai hex sama persis (#0F172A @40%) tapi kini lewat token; (2) **token hover baru** di `index.css`: `--color-status-danger-hover: #b91c1c` & `--color-status-ready-hover: #15803d` → dipakai `Button.tsx` variant danger (sekaligus `text-white` → `text-text-on-accent`) & tombol toggle Habis/Tersedia `MenuManagement`; (3) **toast error Kasir**: `shadow-modal` → `shadow-dropdown` (level 2 lebih tepat untuk notifikasi transien, bukan modal) + `text-white` → token + tambah `z-50`; (4) **tombol KDS** `rounded-md` → `rounded-lg` (8px sesuai skala button DESIGN.md). Verifikasi: build ✓, lint ✓ (warning AuthContext sudah ada sebelumnya), 18/18 tes ✓. Tidak ada perubahan visual yang terlihat mata (semua nilai warna identik) — murni disiplin token.
- **Menu Varians — arsitektur varian menu (sesuai PRD §4.1)**: Dua opsi: (A) tabel terpisah `menu_item_variants` (ormalisasi, admin bisa CRUD varian sendiri) vs (B) JSON/enum statis (sederhana tapi tidak fleksibel). Keputusan: **opsi A** — tabel terpisah karena PRD §4.1 "menu item punya varian dengan harga berbeda (Contoh: Nasi Goreng: Rp25.000 (Reguler), Rp35.000 (Besar))" menunjukkan varian itu data domain, bukan sekadar label. Backend: `MenuItemVariant` (name, price, available, position), relasi `hasMany` di `MenuItem`, eager-load via `whenLoaded` di Resource. `MenuItemController::store/update` sinkron varian (delete+recreate posisi). `OrderController::store` terima field `variantName`, resolve harga varian + cek available; harga varian menggantikan harga dasar (bukan ditambah). Frontend: cart keying = `menuItemId-variantName` (compound key) sehingga item yang sama dengan varian berbeda jadi baris terpisah; pill varian di menu panel + pelayan + menu QR; admin CRUD varian di modal edit menu (tambah/hapus/nama/harga/aktif).
- **Menu Varians — desain pill UI**: Varian ditampilkan sebagai pill horizontal di bawah nama item (bukan dropdown select). Keputusan: pill → user kasir/pelayan cukup 1 klik (tap) untuk pilih varian, lebih cepat dari dropdown yang butuh 2 interaksi (buka + pilih). Item tanpa varian tetap tampilkan tombol `+` langsung (tidak ada pill). Varian yang `available=false` tetap tampilkan tapi dengan opacity rendah + tooltip "Tidak tersedia".
- **Void Order — aturan dan UI**: PRD §5.3: "Batal pesanan sebelum bayar: status `dibatalkan`, meja kembali kosong." Aturan implementasi: hanya status `menunggu-konfirmasi` atau `diproses` yang bisa dibatalkan; `selesai` tidak bisa dibatalkan (sudah dibayar); role admin + kasir. Backend: `PATCH /orders/{order}/void` dengan validasi status. Frontend: tombol "Batalkan" di `OrderCard` (KasirQueuePanel) dengan `window.confirm()`; badge "Batal" merah (`bg-status-danger/15`) di riwayat; filter status di TransactionHistory; event `voided` → KDS otomatis hilangkan ticket (sudah difilter `status === 'diproses'`).
- **Void Order — tidak perlu refund mechanism**: Untuk prototype 1 outlet, void hanya mengubah status order + meja. Refund tunai/QRIS tidak ada di sistem (kasir memproses refund manual di luar aplikasi). Log void hanya di status order (audit trail sederhana).
- **TopNavBar responsif untuk HP Pelayan**: Halaman Pelayan sudah mobile-first sejak awal (`max-w-md`, grid meja 3 kolom, target sentuh ≥44px), tetapi `TopNavBar` tidak responsif — logo 32px + link nav + nama user + 2 tombol teks dipaksa 1 baris ≈570px > layar HP 360–414px → pasti meluber di HP asli. Keputusan device untuk role Pelayan = **HP mobile** (data APJII 2026: smartphone 84,31% akses internet vs tablet 2,39%; penetrasi HP 116% populasi — rasio ±35:1; pelayan restoran lokal bawa HP, bukan tablet). Perubahan hanya di `components/TopNavBar.tsx`, desktop ≥768px tidak berubah sama sekali: (1) padding `px-6` → `px-4 md:px-6`; (2) logo `text-display` → `text-heading md:text-display`; (3) link navigasi `hidden md:flex` — di mobile disembunyikan (tiap role non-admin cuma punya 1 halaman relevan); (4) nama+role `hidden md:block`, diganti avatar bulat inisial (`bg-accent-tint`, huruf pertama nama) via `md:hidden`; dot status online `hidden sm:inline-block`; (5) tombol Ganti Password & Keluar jadi ikon-only di mobile (SVG lock/logout inline, `aria-label` + `title`) dengan label teks `hidden md:inline`; (6) **hamburger menu** (SVG 3 garis, `relative` wrapper + dropdown absolute `shadow-dropdown`) muncul HANYA jika `visible.length > 1 && md:hidden` — yaitu admin di HP, karena admin kehilangan navigasi antar-halaman kalau link disembunyikan; klik link menutup menu. Verifikasi: build ✓, lint ✓, 18/18 tes frontend ✓. Uji visual disarankan via DevTools device toolbar (375px) atau HP asli satu WiFi (`vite server.host` sudah `true` → `http://<ip-laptop>:5173/pelayan`).
- **Fix label meja meluber di HP (uji HP asli)**: Temuan user saat uji HP: label status **"Perlu Dibersihkan"** keluar dari kotak sel meja di peta meja Pelayan. Akar masalah: layar 375px − padding konten 32px = 343px; grid 3 kolom gap-12px → sel ±106px; dikurangi `p-4` (32px) ruang teks tinggal ±74px, sedangkan kata "DIBERSIHKAN" (uppercase + tracking-wide, 13px) butuh ±86px — kata tunggal tidak bisa wrap → overflow. Fix di `pages/pelayan/TableSelect.tsx`: (1) padding sel `p-4` → `p-2 sm:p-4` (ruang teks jadi ±93px ≥ 86px); (2) grid gap `gap-3` → `gap-2 sm:gap-3`; (3) label span tambah `max-w-full break-words text-center leading-tight` sebagai pengaman — di layar lebih sempit kata wrap ke baris kedua di dalam kotak, bukan meluber. Preventif juga di `pages/kasir/TablePickerModal.tsx` (modal pilih meja Kasir punya masalah serupa di layar sempit): grid `grid-cols-4 gap-3` → `grid-cols-2 gap-2 sm:grid-cols-4 sm:gap-3` + label status span dapat perlakuan sama (`max-w-full break-words text-center leading-tight`). Desktop/tablet ≥640px tampilan persis seperti sebelumnya. Verifikasi: build ✓ lint ✓ 18/18 tes ✓.
- **Fix WebSocket gagal dari HP + IP laptop berubah (uji HP asli)**: Console HP menunjukkan WS error ke `ws://192.168.1.5:8080` & fallback `wss://192.168.1.5`. Investigasi: (1) Reverb sehat (`GET /up` → `{"health":"OK"}`, listen 0.0.0.0:8080); (2) **akar utama: IP laptop berubah** via DHCP (Wi-Fi 2) dari `192.168.1.5` → `192.168.1.213`, sehingga `.env` frontend yang hardcode HOST lama menunjuk alamat mati; (3) masalah kedua: WS dev langsung ke port 8080 rentan diblokir firewall Windows & pecah tiap IP berubah. **Solusi arsitektural** (`services/echo.ts`): WS kini **selalu same-origin** (`window.location.hostname` + port halaman) → lewat proxy `/app` yang sudah ada di `vite.config.ts` (`ws:true` → `127.0.0.1:8080`), pola identik dengan production (wss:443 via Nginx). Env `VITE_REVERB_HOST/PORT/SCHEME` menjadi override opsional; `.env` & `.env.example` frontend dirapikan (hanya `VITE_API_URL` + `VITE_REVERB_APP_KEY`). Keuntungan: ganti WiFi/IP tidak merusak real-time, tidak perlu buka port 8080 di firewall untuk uji HP. **Bug backend ikut ketemu saat uji**: (a) tabel `settings` (fitur PPN otomatis, commit d89d312) belum dimigrasikan di DB lokal → semua create order 500 ("Table 'dineflow_pos.settings' doesn't exist") → fix `php artisan migrate --force`; (b) `OrderController::store` error "Undefined array key tableId" bila request TIDAK mengirim field `tableId` (frontend selalu kirim null jadi tak pernah kelihatan) → fix `$validated['tableId'] ?? null` (pola sama dengan fix QRIS cashReceived). **Verifikasi end-to-end lewat proxy**: listener Node pusher-js subscribe channel `orders` via `ws://127.0.0.1:5173/app/dinflow-pos-key` → CONNECTED; POST `/api/orders` self-order → event `OrderStatusChanged` action=created diterima ≤2 detik. Catatan tes: skrip pusher-js mentah WAJIB `forceTLS:false` (default true → coba wss yang tak ada di dev). DB di-reset ke demo (`migrate:fresh --seed`). Pesan console lain yang muncul saat uji TIDAK berbahaya: saran install React DevTools (info), CSP `eval` warning (mode dev Vite), overlay "Press ctrl i" (fitur browser/AI assistant, bukan aplikasi).
- **Laporan penjualan: rincian per metode bayar + rentang tanggal custom**: Permintaan pengguna agar laporan lebih mendekati POS resto umum. Dua fitur digabung di halaman Laporan Penjualan yang sama (tanpa menu baru): (1) kartu **"Per Metode Bayar"** berisi 2 baris Tunai & QRIS (omzet + jumlah transaksi) — revenue memakai `order->total` **termasuk PPN** karena itu uang yang benar-benar diterima kasir/GoPay; (2) **rentang tanggal custom** dua date-picker (`startDate`/`endDate` format YYYY-MM-DD, validasi regex di backend) + tombol "Reset tanggal". Backend `SalesSummaryController` direfactor: logika filter periode lama + rentang baru diekstrak ke method privat `applyFilters()` yang dipakai bersama `index()` dan `export()` (CSV ikut menghormati rentang). Respons API bertambah field `paymentBreakdown: {tunai: {revenue,count}, qris: {...}}`. Frontend: type baru `PaymentMethodStat` & `SalesDateRange`, kontrak `Api.getSalesSummary/exportSalesReport` tambah parameter range opsional (implementasi httpApi pakai URLSearchParams; mockApi ikut signature), helper privat `salesQuery()`. UI toolbar berubah `flex-wrap` agar date-picker rapi di layar sempit. Tanpa migrasi database (data payments sudah ada). **Tes backend baru `SalesSummaryTest`**: breakdown metode bayar benar (2 tunai Rp35rb + 1 qris Rp35rb) dan filter rentang tanggal mengecualikan order lama; catatan: kolom `payments.paid_by` ternyata FK ke users bukan teks → tes pertama gagal FK constraint lalu diperbaiki. Hasil verifikasi: backend 7/7 tes (18 assertion), FE build+lint+18 tes lulus, E2E curl: order→confirm→bayar tunai 36.000+PPN → laporan `tunai:{revenue:39600,count:1}` ✓. Insiden lingkungan saat sesi ini: MySQL/Redis/Reverb/Vite sempat mati semua (API 500 karena broadcast Reverb gagal → order 500 padahal data tersimpan); semua dihidupkan ulang manual via Start-Process (mysqld dengan --defaults-file Laragon, redis-server, reverb:start, npm run dev).
- **Riwayat Transaksi Kasir & Admin (fitur audit trail)**: Sesuai rencana terkunci di `docs/rencana-perbaikan.md` — tanpa nama kasir (v1), tanpa pagination, unlimited. **Backend** (tanpa migrasi): `OrderController::index` eager load `payment`; `OrderResource` expose `payment` via `PaymentResource` (`whenLoaded`, jadi endpoint lain tidak berubah bentuk). Tes baru `test_orders_index_includes_payment_data` (9 tes total). **Bug fix penting yang ditemukan saat E2E**: sejak B3 rute `POST /orders` publik, `$request->user()` di `OrderController::store` selalu null (default guard `web`, bukan sanctum) sehingga **semua** order tercatat source `self-order`/status `menunggu-konfirmasi` — termasuk dari Kasir/Pelayan (rusak diam-diam; alurnya tetap bisa jalan karena kasir mengonfirmasi order sendiri). Fix: `auth('sanctum')->user()` untuk deteksi role; E2E curl membuktikan order kasir kembali `source:kasir, status:diproses`. **Frontend bersama**: type `Order.payment?`; `ReceiptModal` pindah ke `components/` (dipakai lintas role, import via git mv agar history terjaga); helper `lib/receipt.ts` `orderToReceipt()` menggantikan duplikasi build-ReceiptData di KasirPage (subtotal = Σ items, tax = total−subtotal, method/change dari payment). **Kasir**: panel queue kini dua tab pill level-1 "Pesanan Aktif" (sub-tab Masuk/Nota tidak berubah) | "Riwayat" (keputusan user: struktur nested); riwayat default chip "Hari ini" + opsi "Semua"; baris = jam, no.order, meja/sumber (take-away → label sumber), total, badge metode (Tunai=hijau status-ready, QRIS=biru status-done); klik baris → cetak ulang. Riwayat otomatis real-time karena derived dari state `orders` yang sudah di-refresh listener channel `orders`. **Admin**: tab navbar "Riwayat Transaksi" SETELAH "Laporan Penjualan"; komponen self-contained `TransactionHistory.tsx` (fetch orders+settings sendiri, pola SalesReport): grid tabel (Waktu/No.Order/Meja-Sumber/Item/Total/Metode), filter client-side rentang tanggal + cari no.order + dropdown metode + tombol Reset filter, klik baris → cetak ulang struk (keputusan user), subscribe channel `orders` action=`paid` → refetch + leaveChannel unmount. **Verifikasi**: `php artisan test` 9/9 (28 assertion), FE build+lint+18 tes lulus, E2E curl login→order→bayar tunai 39600 (cash 50000, change 10400)→GET /orders mengandung payment lengkap ✓, DB reset demo (`migrate:fresh --seed`: users=4, tables=8, menus=19, orders=3). Commit ditunda atas permintaan pemilik proyek.
- **Pengaturan real-time + restrukturisasi struk (pasca fitur riwayat)**: Laporan user — ubah pengaturan (logo/PPN/nama) di Admin tidak berpengaruh ke struk Kasir tanpa refresh. Akar masalah: `KasirPage` & `TransactionHistory` memuat settings sekali di mount dan tidak ada event real-time untuk settings (beda dengan menu yang punya `MenuChanged`). **Solusi backend**: event baru `App\Events\SettingsChanged` (pola persis `MenuChanged`: `ShouldBroadcastNow` + `broadcastAs('SettingsChanged')`) di channel publik `settings`; payload dibangun helper privat `SettingController::payload()` yang dipakai bersama oleh `index()`, `update()`, dan `uploadLogo()` sehingga bentuk broadcast = respons `GET /settings` ({taxRate, restaurantName, restaurantAddress, logoUrl}) — dispatch di akhir `update()` & `uploadLogo()`. **Solusi frontend**: subscribe `echo.channel('settings').listen('SettingsChanged', ...)` di `KasirPage` (setSettings langsung) & `TransactionHistory` (cetak ulang selalu identitas terbaru) + cleanup `leaveChannel`; catatan payload: satu properti publik `$settings` → client menerima `{ settings: {...} }` (pola sama seperti `MenuChanged` membungkus `$item`), handler wajib unwrap. **Restrukturisasi struk** atas permintaan user ("sesuai standar resto, ukuran menyesuaikan pesanan"): lebar modal tetap 300px (≈ kertas thermal 80mm), tinggi mengikuti jumlah item secara alami (tanpa fixed height), padding p-5→p-4 & font 12→11px + leading-snug agar rapat; header final setelah dua iterasi keputusan user: logo (jika ada) → **nama restoran di bawah logo** → alamat → no.order/meja/waktu; teks salinan struk ikut aturan sama. **CSS cetak** (`index.css`): `@page { margin: 0 }` menghilangkan header/footer bawaan browser (judul "DineFlow POS"/URL/tanggal di tepi kertas) sekaligus stabilkan posisi; `#print-area` kini `width:302px; margin-inline:auto` terpusat horizontal di atas kertas (sebelumnya `width:100%` melebar penuh A4). Verifikasi: `php artisan test` 9/9, FE build+lint+18 tes lulus, E2E listener WS mentah menerima `SettingsChanged` berisi payload lengkap setelah PUT /api/settings; DB reset demo (PPN kembali 10%).

- **QRIS Statis Semi-Produksi**: Riset GoPay Merchant perorangan dicatat ke `docs/catatan-presentasi.md` (biaya: Rp0 daftar + MDR 0%≤Rp500rb mikro, verifikasi KTP, pencairan instan T+0). Backend: `SettingController::uploadQris()` (pola identik `uploadLogo` — image max 2 MB, simpan `qris` disk public, key `qris_image_url`, dispatch `SettingsChanged`); rute `POST /settings/qris` (admin); `payload()` bertambah `qrisImageUrl`. Frontend: `Settings.qrisImageUrl`; `Api`+ `httpApi`+ `mockApi` tambah `uploadQris()`; `SettingsPage` kartu baru "Gambar QRIS Statis" (preview + upload file, ikut tombol Simpan); `PaymentModal` prop baru `qrisImageUrl`: jika ada → tampilkan gambar asli `<img>` + instruksi cek notifikasi HP merchant + tombol "Sudah Terima Bayaran" (langsung `handleConfirm`, alur 1-langkah); jika tidak ada → simulasi EMVCo lama utuh sebagai fallback; `KasirPage` meneruskan `settings.qrisImageUrl`. Verifikasi: 15 tes backend, FE build+lint+18 tes lulus; uji visual: admin unggah QR → kasir bayar QRIS → gambar tampil + konfirmasi → riwayat badge QRIS. Commit & push: 26 Agustus 2026.
- **Fitur PRD batch 1 — frontend (sesi lanjutan)**: Menuntaskan sisi UI untuk 3 fitur PRD yang backend-nya sudah siap. **(1) PPN otomatis persisten per-transaksi** — Payment kini menyimpan `subtotal`/`ppn_amount`/`total`; `lib/receipt.ts` memakai nilai tersimpan per-transaksi bila ada (fallback hitung ulang), jadi struk selalu konsisten dengan PPN saat bayar bukan ulang pakai tarif terkini. **(2) Void wajib alasan + role dapur** — komponen `VoidOrderModal` (alasan preset dropdown + input bebas, divalidasi "wajib diisi") dipakai Kasir (menggantikan `window.confirm` di `KasirQueuePanel`) dan Kitchen (`OrderTicket` footer tombol "Batalkan"); `api.voidOrder(orderId, reason)`; alasan tampil di riwayat Kasir. **(3) Level Kepedasan 0-5** — item `isSpicy` tanpa varian menampilkan pill `SpicePills` 0-5 (klik langsung tambah di level itu); item pedas ber-varian (Nasi Goreng/Mie Ayam) pilih varian dulu (level 0) lalu atur di keranjang; `useCart` keying `menuItemId-variantName-spiceLevel` (tiap level jadi baris terpisah) + method baru `setSpiceLevel` (re-key + merge bila level sama); stepper "Level Pedas − N +" (0-5) di CartPanel/WaiterOrder/MenuPage; tampil level di ticket KDS, receipt, tracking, OrderCard; Admin toggle "Item Pedas" di form menu + badge "Pedas" di tabel. Keputusan interaksi (user): pill klik-langsung untuk item pedas tanpa varian; pada item ber-varian atur level di keranjang. Verifikasi menyeluruh: `tsc -b` ✓, `vite build` ✓ (453 kB), `vitest run` 18/18 ✓, `php artisan test` 15/15 (40 assertion) ✓, `oxlint` hanya warning lama AuthContext.
- **Konversi 4 mockup Google Stitch ke React + durasi duduk (sesi desain)**: User membagikan mockup terbaru (Table Map, Menu Ordering, Active Orders, Customer Self-Order) dan meminta konversi ke React. **Keputusan desain (jawaban user lewat pertanyaan)**: (1) cakupan diperluas dari "WaiterOrder saja" menjadi semua komponen terkait (TableSelect, WaiterOrder, OrderList, MenuPage); (2) warna **tetap token `DESIGN.md`** — palet Material 3 mockup (primary `#004ac6`, surface `#faf8ff`, dll.) DITOLAK, hanya layout/struktur yang diadopsi; (3) **Bahasa Indonesia** + harga **Rupiah** (`formatRupiah`); (4) ikon **SVG inline** (bukan Material Symbols font, konsisten `TopNavBar`); (5) **skip** sidebar desktop & bottom-nav mobile mockup — tetap `TopNavBar`. Implementasi & logika khusus: **(a) `TableSelect` (Peta Meja Pelayan)** — kartu `aspect-square` + ikon `group` SVG + "N Pax" + **durasi duduk hanya untuk meja `terisi`** (format `mm:ss`, ≥1 jam `h:mm`, auto-refresh `setInterval` 30s); durasi bersumber dari `createdAt` **order aktif terawal** per meja (status `menunggu-konfirmasi`/`baru`/`diproses`) yang dihitung `PelayanPage.seatedAt` → prop baru `seatedAt?: Record<number, number>` (keputusan: **frontend-only**, tanpa kolom `occupied_at` backend, karena prototype & murah; akurasi = waktu order masuk, bukan pelanggan duduk). **(b) `WaiterOrder`** — kartu menu vertikal → **horizontal** (`flex gap-3 p-2`): thumbnail kiri `h-24 w-24` dari `item.imageUrl` (fallback SVG placeholder bila kosong), info+harga+varian/pedas di kanan, tombol `+` kanan bawah. **(c) `OrderList`** — kartu order diberi **garis status kiri** (`bg-status-*` sesuai `orderStatusBar`: menunggu/baru=abu, diproses=kuning, selesai=blue), **elapsed time** di samping meja (auto-refresh 30s), **note item** jadi badge merah kecil, tombol "Tandai Diantar" → "Antarkan". **(d) `MenuPage`** — grid-2-kolom → **kartu horizontal** + overlay "Habis" di thumbnail. **(e) DRY** — `formatElapsed` dipindah ke `lib/format.ts` (dipakai `TableSelect` & `OrderList`). **Backend** (sandera, wajib ikut): seeder order aktif `created_at` diubah dari tanggal Agustus (yang bikin durasi `473:04` — hampir 7,8 jam) menjadi **relatif `now()`** (`subMinutes(47/25/8)`), ORD-0004 `dibatalkan` tetap masa lalu untuk data laporan; `migrate:fresh --seed` dijalankan. Verifikasi: build ✓, lint ✓ (hanya warning lama AuthContext), 18/18 tes FE ✓, curl /tables+/orders menunjukkan T5/T8 `terisi` dengan created hari ini.
- **Pendalaman 4 layar mendekati mockup Stitch (sesi lanjutan 1 Sep)**: Setelah user merasa hasil awal "perubahannya sedikit", dilakukan perbandingan langsung HTML mockup Stitch (di `resto-pos-frontend/`) vs komponen React. Ditemukan elemen mockup yang belum teradopsi & disepakati untuk ditambahkan (user pilih "Perdalam 4 layar"). **Keputusan**: (1) **skip floor tabs** ("Main Floor/Patio") & **tombol Filter** karena aplikasi single-outlet tanpa data floor; (2) tombol OrderList **tetap satu "Antarkan"** (tanpa variasi View Details/Add Note — tanpa modal & backend catatan); (3) Featured Card "Chef's Special" di MenuPage memakai **menu pertama kategori aktif** (tanpa data flag `featured` di backend). Perubahan: **(a) `TableSelect`** — nomor meja `text-subheading`→`text-heading` (accent utk terisi) + label status jadi **pill** (`rounded-full bg-status-*/15 text-status-*`) di bawah kartu via `mt-auto` (bukan absolute, agar tidak meluber — melanjutkan revisi anti-overflow). **(b) `OrderList`** — **ikon jam** SVG di samping `Meja X · durasi`. **(c) `WaiterOrder`** — Review Order bar sticky **sudah ada** (jumlah+total+"Lihat & Kirim"), tanpa perubahan. **(d) `MenuPage`** — komponen `FeaturedCard` baru (gambar besar h-48 + badge harga kanan atas + nama + deskripsi line-clamp-2 + tombol "Tambah ke Pesanan" lebar; menangani varian & item pedas via `SpicePills`; overlay "Habis" utk non-available); daftar reguler `slice(1)` agar unggulan tak duplikat. Verifikasi: build ✓, lint ✓ (hanya warning lama), 18/18 tes FE ✓.
- **Self-order pilih metode bayar + barcode-URL + scan kamera kasir (sesi 2 Sep)**: Menjawab keinginan user memberi pelanggan opsi **"Bayar di Kasir"** (tidak hanya bayar QRIS), dengan **barcode yang bisa di-scan dua arah**: HP pelanggan scan sendiri → buka tracking, DAN kasir scan (alat/kamera) → langsung memanggil tagihan. **Keputusan user**: (1) alur kasir = **kasir bayar order `menunggu`** (memakai tab Masuk + modal Bayar yang sudah ada, tanpa ubah backend); (2) **nama & nomor telepon di-skip** (tanpa migrasi `orders`); (3) barcode berisi **URL** `${origin}/order/{orderNumber}` (bukan nomor polos — yang bikin HP cuma "Salin Teks"); (4) kasir mendukung **ketik manual + barcode gun USB + kamera** (library **`html5-qrcode`**, gratis, tombol ganti kamera Belakang/Depan; kamera wajib HTTPS saat produksi). **Rencana implementasi & temuan**: route publik baru `/order/:orderNumber` → `OrderTrackingPage` (fetch via endpoint publik baru `GET /order-status/{orderNumber}` karena `getOrders` ternyata butuh auth → pelanggan tanpa login akan 401); tracking UI diekstrak jadi `components/OrderTracking.tsx` (dipakai MenuPage + halaman baru); `lib/scan.ts` + 5 tes (`extractOrderNumber` regex `ORD-\d{4}`, tolerir URL/teks). **Bug fix backend ditemukan saat smoke-test**: `PaymentController::store` mengembalikan `new PaymentResource($order->load('payment'))` — resource mengharap model **Payment**, bukan Order → semua field payment (`method`/`status`/`change`/dll.) null → struk kasir rusak (lolos tes karena tes tidak mengecek shape JSON). Fix: `$order->refresh()` + `new PaymentResource($order->payment)` + tambahan assertion JSON di `PrepayTest` (data.method/status/total/change). Verifikasi penuh: backend **23/23** (72 assertion, +tes tracking publik & assertion payment), FE build ✓ lint ✓ **22/22** tes (5 scan baru). E2E curl: self-order → `menunggu` → kasir bayar tunai → `diproses` + meja `terisi` + payment berisi data benar; `/api/order-status/ORD-0008` publik 200 & ORD-9999 404.
- **Perapian alur "Bayar di Kasir" (sesi 2 Sep lanjutan)**: (1) **5.6 di `docs/uji manual.md` diperbaiki** — tes sebelumnya keliru ("pelanggan scan barcode dari HP lain"). Klarifikasi user: barcode di layar pelanggan **hanya untuk kasir** (ketik / barcode gun / kamera di tab Masuk); **tidak ada alur pelanggan scan barcode milik pelanggan lain**; 5.6 jadi tes **live-update sisi pelanggan** (kasir bayar → layar pelanggan otomatis pindah ke tracking `diproses` real-time tanpa refresh; dibuktikan di `MenuPage` listener `order.{orderNumber}` + `setView('tracking')` saat `diproses`). (2) **Tampilkan nominal di layar pelanggan "Bayar di Kasir"** (keputusan user): kartu barcode kini menampilkan **"Total tagihan: Rpxx"** di bawah nomor pesanan (`formatRupiah(payAmount)`, token `DESIGN.md`); nominal di **modal kasir sudah otomatis muncul setelah scan** dari backend (`PaymentModal` `formatRupiah(total)`), jadi nominal TIDAK perlu disisipkan ke dalam barcode (keputusan: sumber kebenaran total = database, bukan isi barcode). Verifikasi: build ✓, lint ✓ (hanya warning lama), **22/22 tes FE ✓**; backend tidak berubah.
- **Fix bug crash Daftar Pesanan di Pelayan setelah bayar QRIS (sesi 2 Sep lanjutan)**: Pengguna melaporkan layar *"Terjadi kesalahan — Terjadi masalah saat menampilkan halaman"* setelah klik **"Saya Sudah Bayar (Demo)"**. **Root cause**: `DatabaseSeeder` masih membuat **ORD-0003 berstatus `menunggu-konfirmasi`** (status legacy sebelum "bayar di muka"), sedangkan map `orderStatusBadge`/`orderStatusBar` di `OrderList.tsx` hanya menangani `menunggu|diproses|selesai|dibatalkan`. Klik demo → `handlePaid` → `setView('orders')` merender `OrderList` → `orderStatusBadge['menunggu-konfirmasi']` = `undefined` → `<StatusBadge variant={undefined}>` → `colorMap[undefined].bg` → TypeError → ErrorBoundary. **Fix**: (1) seeder ORD-0003 status → `menunggu`; (2) `OrderList.tsx` lookup pakai fallback (`?? 'neutral'` utk badge, `?? ''` utk garis); (3) `StatusBadge.tsx` `colorMap[variant] ?? colorMap.neutral` (pertahanan umum, cek konsumen lain: hanya WaiterOrder yg pakai ± variant statis, aman). **Regresi**: `StatusBadge.test.tsx` baru (2 tes: badge status tak dikenal tanpa crash + `OrderList` merender order status legacy tanpa crash) → total **24/24** tes FE; backend **23/23**; DB `migrate:fresh --seed` → ORD-0003 kini `menunggu` (verifikasi live GET /api/orders).
- **Fix getStatus DOKU + tombol Batal overlay QRIS Pelayan (sesi 3 Sep)**: Dua perbaikan kecil sebelum D3 deploy. **(1) `DokuGateway::getStatus` salah invoice** — saat create `createPayment()` mengirim `invoice_number => orderNumber` (mis. `ORD-0005`) tapi `getStatus()` mengirim `invoice_number => reference` (kode `DOKU-XXXX`); karena aturan DOKU QRIS status wajib query memakai invoice yang sama dgn saat buat, server tak menemukan invoice → selalu `pending` → QRIS tak pernah auto-confirm (order macet di tab Masuk). Fix: signature interface `PaymentGateway::getStatus(string $reference, string $invoiceNumber)`, `DokuGateway` memakai `$invoiceNumber` (+hapus `'amount' => null` yang ditolak DOKU di query), `MockQrisGateway` terima+abaikan param, dan `PaymentController::status` meneruskan `$payment->order->order_number`; tidak ada tes yang memanggil getStatus langsung (PrepayTest via HTTP + driver mock) → backend tetap 23/23. **(2) Overlay QRIS Pelayan tak punya tombol Batal** — `QrisPay` mock cuma tombol "Saya Sudah Bayar (Demo)" sehingga pelayan terjebak bila pelanggan tak jadi bayar (kasir sudah punya Batal). Fix: tombol **"Batal"** outline di dalam overlay `PelayanPage` → `setPayOpen(false)`; order kembali `menunggu` (tetap di tab Masuk kasir), `view`/`selectedTable` tak berubah karena overlay menimpa. Klarifikasi: item "redirect /order/{id} 404" yang sempat tercatat keliru — kode aktual memakai fallback mock (`setPayRef(String(order.id))`+`gateway='mock'`) saat checkout gagal di MenuPage/PelayanPage, bukan redirect; edge case ini hanya relevan saat gateway DOKU error (D3) dan ditunda. Verifikasi: `php -l` 4 file ✓, backend **23/23**, FE lint ✓ (hanya warning lama), build ✓, **24/24** tes.
- **Fix perilaku tombol Batal overlay QRIS Pelayan — kembali ke peta meja (sesi 3 Sep)**: Temuan uji manual — setelah klik **"Batal"** di overlay QRIS pelayan, layar terlihat "blank"/polos. Analisis: `showCart` adalah state lokal di `WaiterOrder` yang tetap `true` saat order sukses dikirim (`handleSubmitOrder` → `cart.clear()` → `setPayOpen(true)`), dan overlay saat itu menimpa layar cart. Setelah `payOpen=false`, layar kembali ke `WaiterOrder` dengan `showCart` masih `true` + keranjang kosong → muncul layar penuh **"Keranjang kosong."** (terkesan blank). **Keputusan user: Opsi A** — tombol Batal kini kembali ke **peta meja** dan bersihkan state: `setPayOpen(false)` + `setView('tables')` + `setSelectedTable(null)` + `cart.clear()`. Penting (bukan bug, sesuai rancangan): Batal itu **bukan void** — hanya menutup layar bayar, jadi order tetap status `menunggu` dan **muncul di tab Masuk kasir** (bisa dibayar kasir atau di-void). Verifikasi: FE lint ✓ (hanya warning lama), build ✓, **24/24** tes; Vite hot-reload. Checklist: baris 3.7b baru di `docs/uji manual.md`.
- **Kolom pencarian menu di Pelayan + Self-order (sesi 3 Sep)**: Permintaan user — tambah kolom pencarian di Pelayan & Self-order, konsisten dengan Kasir. Temuan: Kasir (`MenuPanel`+`KasirPage`) sudah punya search (filter `item.name.toLowerCase().includes(q)` dikombinasi `activeCategory`), sedangkan **Pelayan (WaiterOrder)** & **Self-order (MenuPage)** belum punya (hanya filter kategori; MenuPage juga punya FeaturedCard `visibleItems[0]`). **Keputusan user**: tambah di keduanya; filter = **kombinasi kategori + keyword** (pola Kasir); di Self-order, **FeaturedCard disembunyikan saat search aktif** (agar hasil fokus daftar). Implementasi: **Pelayan** — state `search` di `PelayanPage`, `visibleItems` pakai kombinasi kategori+keyword, ruskan `search`/`onSearchChange` ke `WaiterOrder`, input search (ikon kaca) di bawah `CategoryTabs`; teks kosong → "Tidak ada menu ditemukan.". **Self-order** — state `search` di `MenuPage`, `visibleItems` kombinasi, input search di bawah `CategoryTabs`, `FeaturedCard` render hanya bila `search.trim() === ''`. Baik mengikuti token & markup persis `MenuPanel`. Verifikasi: FE lint ✓ (hanya warning lama), build ✓, **24/24** tes; Vite hot-reload.
- **Audit konsistensi frontend menyeluruh — Batch 1: empty state self-order (sesi 3 Sep)**: Audit opsi a+b atas frontend (search, status/meja, token desain, bahasa, responsif) menemukan self-order (`MenuPage`) tidak punya umpan balik saat hasil filter maupun pencarian habis — layar polos, padahal Kasir (`MenuPanel`) & Pelayan (`WaiterOrder`) sudah punya **"Tidak ada menu ditemukan."**. **Keputusan user**: urutan eksekusi mulai Batch 1 (empty state) dulu, lalu dilanjut batch lain; commit terpisah per batch. Fix: di `<ul>` `MenuPage`, saat `visibleItems.length === 0` render `<li className="py-10 text-center text-body text-text-secondary">Tidak ada menu ditemukan.</li>` (pola persis MenuPanel/WaiterOrder). Commit `05a9fe8` ter-push. Verifikasi: lint ✓, `tsc -b` ✓, 24/24 tes.
- **Audit konsistensi — Batch 2: sentralisasi status ke `lib/statusConfig.ts` (sesi 3 Sep)**: Temuan audit: pemetaan status diduplikasi di beberapa file (risiko drift), ada **bug nyata**: order `dibatalkan` di `OrderTracking` jatuh ke `default:'new'` → badge "Baru" abu, bukan "Dibatalkan" merah. **Keputusan**: (1) ekstrak semua pemetaan ke **`lib/statusConfig.ts`** sebagai sumber tunggal: `orderStatusBadge` (`menunggu:new, diproses:cooking, selesai:done, dibatalkan:cancelled`), `orderStatusLabel` (Menunggu Pembayaran/Sedang Dimasak/Selesai/Dibatalkan), `orderStatusBar` (warna garis kiri), `itemStatusBadge` (baru:new, dimasak:cooking, siap:ready, diantar:done), `itemStatusLabel` (kapital Indonesia), `tableStatusLabel` (Kosong/Terisi/Perlu Dibersihkan). File konsumen: `OrderList` (hapus `orderStatusBadge`/`orderStatusBar` lokal), `OrderTracking` (hapus `itemBadge`/`orderStatusLabel` lokal + fungsi `orderStatusBadge()`), `TablePickerModal` (`tableStatusLabel`). (2) **B2**: `OrderList` `diproses: 'neutral'` → `'cooking'` (kuning, seragam OrderTracking & bar). (3) **B3**: label meja kasir `'perlu-dibersihkan' ? 'Bersihkan' : table.status` → `tableStatusLabel` (raw lowercase → Kapital). **Pengecualian netral**: `dibatalkan` di OrderList dulu `'danger'` → di config kini `'cancelled'` (keduanya merah di StatusBadge, label sama "Dibatalkan" — visual identik, lebih konsisten). Commit `36d70fc` ter-push. Verifikasi: lint ✓, `tsc -b` ✓, 24/24 tes (inkl. regresi legacy-status).
- **Audit konsistensi — Batch 3: kapitalisasi label status item & order (sesi 3 Sep)**: Temuan audit: status item tampil **raw lowercase** ("baru"/"dimasak") di OrderTicket, OrderTracking, OrderList — inkonsisten visual. **Keputusan user**: masukkan sekarang, sesuaikan yang terbaik & konsisten. Fix: ganti `{item.status}` → `{itemStatusLabel[item.status] ?? item.status}` (kapital "Baru/Dimasak/Siap/Diantar") di **OrderTicket** (`text-kitchen-meta` badge), **OrderTracking** (item badge), **OrderList** (item badge). Bonus konsistensi (audit menemukan dalam area sama): badge **order-level** di `OrderList` masih `label={order.status}` (raw) → `orderStatusLabel[order.status] ?? order.status` ("Menunggu Pembayaran"/"Sedang Dimasak") — seragam dengan OrderTracking. Hasil akhir: tidak ada lagi tampilan `{item.status}`/`{order.status}` mentah di mana pun. Commit `3347074` ter-push. Verifikasi: lint ✓, `tsc -b` ✓, 24/24 tes.
- **Audit konsistensi — Batch 4: token visual (overlay & font-num) (sesi 3 Sep)**: Temuan audit token: **D1/D2** overlay QRIS Pelayan adalah satu-satunya yang memakai `bg-black/60` + `shadow-xl` (semua modal lain `bg-text-primary/40` + `shadow-modal`) → disamakan. **D4** harga **varian** menu (Reguler/Besar) tampil tanpa font monospace `font-num`, padahal aturan DESIGN.md "angka/harga pakai `font-num`" → bungkus tiap `formatRupiah(v.price)` dalam `<span className="font-num">` di 4 tempat: `MenuManagement` (pill varian tabel), `MenuPage` (kartu menu baris + FeaturedCard), `WaiterOrder` (kartu menu). **D3 & D5 dikeluarkan dengan alasan teknis**: (a) `bg-white` di `ReceiptModal.#print-area` (struk thermal) & `MenuPage` QR (background QR **wajib terang** agar scan andal) adalah kebutuhan fungsional, bukan pelanggaran token; (b) `text-[10px]/[11px]` badge di layar padat (KasirQueuePanel, TransactionHistory, MenuManagement tags) adalah ukuran ringkas yang **disengaja & konsisten** — memaksakan ke token `caption` 13px justru menggerombolkan; struk cetak 11px juga harus tetap. Commit `dbccbfb` ter-push. Verifikasi: lint ✓, `tsc -b` ✓, 24/24 tes.
- **Audit konsistensi — Batch 5: terjemahan bahasa Indonesia (sesi 3 Sep)**: Temuan audit bahasa: sejumlah istilah Inggris tersisa di UI. **Keputusan user**: terjemahkan semua (ikut semua polishing minor) & samakan istilah keranjang → "Keranjang". Perubahan: "Take Away"→"Bawa Pulang" (TablePickerModal "Take"/"Away" per baris→"Bawa"/"Pulang" + KasirPage label + OrderTicket "Meja X / Bawa Pulang" + `lib/receipt.ts`); "Hold"→"Tahan" (`CartPanel`); "Lihat Pesanan"/"Pesanan Anda"→"Lihat Keranjang"/"Keranjang Anda" (MenuPage, konsisten Kasir/Pelayan); "Edit"→"Ubah" (+title modal "Ubah X", MenuManagement); "Export/Exporting"→"Ekspor/Mengekspor" (SalesReport); "Reset filter"→"Atur Ulang Filter" (TransactionHistory); "Reset"+confirm "Reset password...ke 1234?"→"Atur Ulang"/"Atur ulang password..." (StaffManagement — baris overlap butuh perbaikan manual, dikembalikan utuh); "Self-Order"→"Pesan Mandiri" (KasirQueuePanel, TransactionHistory); "No. Order"→"No. Pesanan" (TransactionHistory); "e.g."→"mis." (MenuManagement placeholder); "role staf"/"mereset password"→"peran staf"/"mengatur ulang password" (AdminPage error msg); "Login gagal"→"Masuk gagal" (LoginPage, konsisten tombol "Masuk"). **Dikembangkan (C12 not-a-bug)**: "item count" (jumlah baris menu) vs "porsi" (total porsi) adalah **dua metrik berbeda** — bukan inkonsistensi, dibiarkan. Commit `fa6bbd9` ter-push. Verifikasi: lint ✓, `tsc -b` ✓, 24/24 tes; seluruh 5 batch audit tuntas & ter-push.
