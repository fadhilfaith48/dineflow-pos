/**
 * Konstanta bersama aplikasi (bukan data tiruan).
 * Tax rate default dipakai fallback keranjang/struk sebelum settings dari server termuat;
 * DEFAULT_PASSWORD hanya untuk tombol isi-otomatis akun demo di halaman login.
 */
export const TAX_RATE = 0.1

export const DEFAULT_PASSWORD = '1234'
